# AGENTS.md — قرارداد توسعه عامل‌ها

> این فایل قرارداد الزامی‌هر عامل (AI یا انسان) است که در این مخزن کد می‌نویسد.
> مبنای تصمیم‌ها: گزارش ممیزی سند «پیشنهاد پلتفرم عملیاتی سازمانی پلاگین‌محور» + تجربه عملی فاز پایلوت.
> تاریخچه کارها: `worklog.md` — **قبل از هر تسک بخوانید، بعد از هر تسک بخشی اضافه کنید.**

## پروژه در یک پاراگراف

پلتفرم عملیاتی سازمانیِ پلاگین‌محور برای هلدینگ ۴ شرکت کاشی و سرامیک (آراد سرام پیشرو، صنایع کاشی اصفهان، کاشی نیلو، لیان کاوش فراز) + شرکت هلدینگ. معماری: **مونولیت ماژولار** با **رجیستری پلاگین دیتابیس‌محور** و **تاکسونومی سه‌لایه** (ADR-008: بستر/عملیات/هوشمندی — ۳۱ پلاگین، پوشش کامل ۲۶ پلاگین سند منبع). چشم‌انداز: پایلوت ۹۰ روزه → گیت تصمیم کمی → فعال‌سازی تدریجی پلاگین‌ها.

**وضعیت اجرا (بازنگری ۱۴۰۵/۰۶/۱۰):** فاز P0 کامل (۲۶/۲۶) · گیت G0 سبز. فاز P1: **کامل ۳۷/۳۷**. فاز P1.5: **کامل ۱۴/۱۴**. فاز P2.5: **کامل ۱۰/۱۰** (فاز بسته). فاز P2: **۱۰/۲۶** (T4 متن پاسخ · T6 قالب شماره · T7 چاپ سربرگ · T10 مهلت ارجاع · T11 یادآور · T13 گزارش هفتگی · T14 AI ویرایش‌پذیر · T16 OCR · T20 اکسل · T5 جستجوی تمام‌متن). **P4-T0 موتور کدگذاری ساختارمند اجرا شد** (۴۳+۱۹ سنجه). صف بازسازی: R1+R2+R3+R5+R6+R7+R8 ✅. **لایه سوم ماندگاری فعال شد: مهاجرت کامل SQLite→Neon Postgres (۱۴۰۵/۰۶/۱۰ — connection string کارفرما رسید): ۵۴٬۵۶۱ ردیف/۳۴ جدول + بازسازی ایندکس FTS روی tsvector/GIN + رگرسیون ۱۳ باتری سبز.** **P0.5-T3 ✔ بسته شد ۱۴۰۵/۰۶/۱۰**: scrypt غیرمسدودکننده + محدودساز نرخ ماندگار در DB (LoginAttempt، CREATE مستقیم) + گارد CSRF `src/proxy.ts` (پیمان Next 16 — middleware منسوخ) روی همهٔ mutationهای API + CI مینیمم `.github/workflows/ci.yml` + ماتریس پوشش `product/05-test-coverage.md` (باتری اختصاصی ۱۹/۱۹ + رگرسیون ۹ باتری سبز + آزمون مرورگر زنده). **گام بعدی: بازسازی seed:big (کشف: دادهٔ بزرگ جایگزین شده بود؛ t5-fts/list-contract داده‌وارده) → تکمیل باتری golden (به‌تعویق T1) → R9 (شماره‌گذاری per-type نامه/سند)** — لایه‌های ماندگاری: دیسک+GitHub+Neon هر سه فعال · **P0.5-T1 ✔ بسته شد ۱۴۰۵/۰۶/۱۶**: تراکنش‌داری `applyDocToStock` + گارد 409 `actOnLetter` + ۱۶ تست واحد + ۲۳ سنجهٔ e2e زنده · **P0.5-T2 ✔ بسته شد ۱۴۰۵/۰۶/۱۶ (CMD-011)**: رجیستری ۲۹→۴۱ ردیف هم‌راستا با فهرست ۳۳ مصوب v1.1 (validate ۳۳/۳۳) + `Warehouse.kind` سه‌گانه (فیزیکی/مجازی/پای‌کار) + شناسنامهٔ پالت ۱۴کاراکتری (`/api/coding/pallet`) + شمارندهٔ پویای سایدبار · **ADR-010 پروتکل اجرای نشست (بررسی→بیانیه→اجرا→تثبیت→پیشنهاد) از این پس الزامی است**

**نقشه راه استراتژیک:** سند «نقشه راه ۱۰ فاز اصلی» در `download/نقشه راه ۱۰ فاز اصلی ideaone.docx` (postcheck ۹/۹) ده فاز استراتژیک F1..F10 را با ۱۰۸ گام و ≈۳٬۱۰۰ وظیفه تعریف می‌کند؛ نگاشت: F1=P1+P2 · F2=P3+P4 · F3=P5 · F4=P6 · F5=P8+بازرگانی · F6=P7 · F7=P9 · F8=AI Platform (جدید) · F9=استودیو هوشمند (جدید) · F10=P10+چت؛ گیت‌های جدید G6/G7/G8. پس از تأیید کاربر، `docs/roadmap/00-master.md` دوطبقه (F استراتژیک + Pn-Tkk اجرایی) بازنویسی می‌شود.

**بهره‌برداری روزانه**: تست واحد مسیرهای بحرانی: `bun run test:unit` (۱۶ سنجه — mock بدون DB زنده) · راستی‌آزمایی زندهٔ تراکنش/گارد 409: `bun scripts/test-p05-t1-e2e.ts` (۲۳ سنجه) · **انطباق رجیستری با فهرست ۳۳ مصوب: `node scripts/validate-modules.mjs` (۳۳/۳۳ PASS — پس از هر seed)** · **شناسنامهٔ پالت ۱۴کاراکتری: `bun scripts/test-pallet.ts` (۱۵ سنجه)** · تست‌های خودکار: `bunx tsx scripts/test-login-security.ts` · `bunx tsx scripts/test-users-bundle.ts` (کاربر/رمز/نشست/دستگاه) · `bunx tsx scripts/test-rbac.ts` (ماتریس نقش×عملیات + CSV/BOM) · `bunx tsx scripts/test-warehouses-crud.ts` (انبار سه‌گانه) · `bunx tsx scripts/test-coding.ts` (موتور کدگذاری — ۴۳ سنجه) · `bunx tsx scripts/test-coding-ui.ts` (کدساز در مرورگر — ۱۹ سنجه؛ نیازمند seed-code-schemes) · `bunx tsx scripts/test-ux-foundation.ts` (سه‌گانه UX) · `bunx tsx scripts/test-list-contract.ts` (قرارداد فهرست‌های سرور) · `bunx tsx scripts/test-record-pages.ts` (APIهای صفحه رکورد P1.5) · `bun scripts/test-indexes.ts` (نمایه‌ها/EXPLAIN) · `bunx tsx scripts/test-perf.ts` (کارایی صفحات کلیدی — پس از seed:big) · `bunx tsx scripts/test-gate-metrics.ts` — گزارش جلسه گیت: `bunx tsx scripts/gate-report.ts` — اسنپ‌شات دیتابیس: `bun run db:snapshot` — داده حجمی: `bun run seed:big`.

## محیط سندباکس — قواعد غیرقابل مذاکره

| قاعده | دلیل |
|---|---|
| فقط مسیر `/` (src/app/page.tsx) قابل نمایش به کاربر است — route جدید نسازید | گیت‌وی فقط پورت ۳۰۰۰ را نشان می‌دهد |
| هرگز `bun run build` یا `bun run dev` نزنید — dev سرور خودکار سیستم است | فرایند نظارتی محیط |
| `z-ai-web-dev-sdk` فقط سمت سرور (route/service با `server-only`) | کلید در کلاینت لو می‌رود |
| API و WebSocket فقط با مسیر نسبی + `XTransformPort` — مطلق ممنوع | فوروارد Caddy |
| سوکت WebSocket همیشه `io('/?XTransformPort=3003')` با path `/` | قاعده گیت‌وی |
| پروسه پس‌زمینه باید با الگوی double-fork اجرا شود: `( cd dir && setsid cmd >> log 2>&1 < /dev/null & )` | پروسه‌های معمولی بین دستورات کشته می‌شوند |
| سرویس‌های جانبی = مینی‌سرویس Bun مستقل در `mini-services/` با پورت ثابت | فقط یک پورت عمومی داریم |
| دیتابیس زنده = **Neon Postgres ابری** (endpoint pooler در `.env` — راز هرگز کامیت نشود) · فایل `db/custom.db` = آرشیو بازیابی (منبع حقیقت نیست) | لایه سوم ماندگاری: ریست سندباکس روی داده بی‌اثر (۱۴۰۵/۰۶/۱۰) |
| فایل‌های کاربر فقط در `download/` تحویل می‌شوند | مسیر قابل دانلود |

### پروتکل ماندگاری نشست (الزامی — درج ۱۴۰۵/۰۶/۱۰ پس از دو رخداد برگشت محیط)

| زمان | اقدام الزامی | دلیل |
|---|---|---|
| **شروع هر نشست** | (۱) آخرین ورودی `worklog.md` بخوان (۲) `git log --oneline -5` + `git status --short` (۳) سلامت سرور `curl 127.0.0.1:3000/api` (۴) تطبیق «آخرین وضعیت مکتوب» با دیسک — هر ناهماهنگی = گزارش به کارفرما پیش از هر کاری | محیط گاهی به ایستگاه git قبلی بازمی‌گردد (رخداد ۰۶/۰۹: یک روز کار حذف شد) |
| **پایان هر بسته** | `bun run check` سبز → `git add -A && git commit` (Conventional، فارسی، شناسه وظیفه) — **هیچ بسته سبزِ بی‌کامیت رها نشود** | ایستگاه‌های بازیابی فقط تا آخرین کامیت پایدارند |
| **پایان هر نشست** | کامیت نهایی + ورودی worklog با «Stage Summary» + اگر ریموت GitHub فعال است: `git push` | قطعی‌ترین لایه ماندگاری |
| **ریموت فعال شد** | پس از هر بسته `git push origin main`؛ در شروع نشست `git fetch` + مقایسه | کد از sandbox مستقل می‌شود |
| **دیتابیس خارج (Neon)** | `.env` به Postgres خارجی اشاره می‌کند: رمز اتصال هرگز کامیت نشود (`.env` gitignore است؛ نمونه در `.env.example`) · ⚠ محیط سندباکس گاهی `DATABASE_URL` قدیمی (file:…) را به‌صورت متغیر تزریقی می‌آورد که بر `.env` اولویت دارد → **همیشه با `unset DATABASE_URL` (یا داخل subshell) اجرا کنید**؛ گارد db.ts خطای واضح فارسی می‌دهد · اجرای همه اسکریپت‌ها/سرور: `( unset DATABASE_URL; … )` | جلوگیری از نشت راز + خطای پروتکل |

### پروتکل اجرای نشست — ۵ مرحله الزامی (ADR-010 — فرمان کارفرما ۱۴۰۵/۰۶/۱۶)

> متن کامل استدلال و گزینه‌های رد‌شده: [`docs/adr/ADR-010-session-protocol.md`](docs/adr/ADR-010-session-protocol.md). نقض هر مرحله = باگ فرایندی، نه سلیقه.

| مرحله | اقدام الزامی | شرط عبور |
|---|---|---|
| ۱. **بررسی** | ممیزی شروع نشست (جدول ماندگاری بالا) + تعیین «وظیفه بعدی صف» از آخرین ورودی worklog + تطبیق وضعیت مستندات با دیسک | ناهماهنگی = گزارش به کارفرما پیش از هر تغییری |
| ۲. **بیانیه** | فهرست وظایف شماره‌دار **پیش از اجرا**؛ هر وظیفه = **دلیل + خروجی + معیار پذیرش** (+ اندازه S/M/L) | کارِ بیانیه‌نشده ممنوع؛ وظیفه تازهٔ ظاهرشده هم ابتدا بیانیه می‌شود |
| ۳. **اجرا** | بسته‌های کوچک؛ تأیید فوری هر وظیفه (تست/check/شاهد عینی)؛ وظیفه بزرگ در همان نقطه خرد می‌شود | هر وظیفه پیش از رفتن به بعدی، شاهد سبز دارد |
| ۴. **تثبیت** | به‌روزرسانی مستندات وضعیت‌دار (فایل فاز/ماتریس ۰۴) **هم‌کامیت با کد** + `bun run check` سبز + کامیت Conventional + ورودی worklog (قالب موجود) | بستهٔ سبزِ بی‌کامیت رها نمی‌شود (جدول ماندگاری) |
| ۵. **پیشنهاد** | پایان نشست: ۲-۳ گام بعدی با دلیل + فهرست صریح حکم‌های باز کارفرما | کارفرما بدون پرسش، «گام بعدی صف» را از آخرین ورودی worklog می‌خواند |

**قانون زبان گفت‌وگو (حکم صریح کارفرما ۱۴۰۵/۰۶/۱۶ — «طبق قوانین همیشه با من فارسی صحبت کن»):** همهٔ پیام‌ها، بیانیه‌های وظایف، گزارش‌ها و خلاصه‌های نشست با کارفرما **همیشه به فارسی** نوشته می‌شوند؛ شناسه‌های فنی (کد، Plugin ID، مسیر فایل) به همان صورت استاندارد لاتین باقی می‌مانند.

**قوانین تاب‌آوری سندباکس (الزامی):**

| قانون | متن | ریشه |
|---|---|---|
| چت سند نیست | گزارش/تصمیم/حکم مهم ⇒ در همان نشست به مستند دائمی تبدیل شود؛ فقط-در-چت = از‌دست‌رفته | unified-roadmap-2026-09-01 |
| دو شکست ابزار | دو شکست متوالی همان ابزار ⇒ توقف + ثبت در worklog + اطلاع کارفرما؛ تلاش سوم کور ممنوع | سه خطای متوالی ثبت worklog |
| بسته کوچک | هر نشست ≤ ۳-۵ وظیفه قابل‌تکمیل؛ نصف‌شدن نشست ⇒ «وضعیت ادامه» در worklog | زمینهٔ محدود نشست |
| بودجهٔ زمینه | خواندن هدفمند (tail/grep/بازه) به‌جای بازخوانی کل فایل‌های بزرگ | worklog ~۱۸۰KB / AGENTS.md حجیم |
| وضعیت صادقانه | ادعای «انجام‌شده» ⇒ شاهد (تست سبز/کامیت/فایل)؛ بدون شاهد ممنوع | ممیزی‌های متوالی |

## معماری — مونولیت ماژولار

```
src/
├── app/            # پوسته نازک: page.tsx + api/**/route.ts (فقط گارد + delegate)
├── instrumentation.ts  # قلاب بوت سرور → راه‌اندازی هسته زمان‌بند (ADR-009)
├── core/           # هسته پلتفرم — ۱۸ سرویس سند منبع (ADR-009)
│   ├── auth/           # نشست scrypt + httpOnly cookie
│   ├── tenancy/        # چندمستأجری: scopeCompanyIds («شرکت = مستأجر»)
│   ├── events/         # Outbox (الگوی رویداد، بدون message broker)
│   ├── notifications/  # notify() + push بلادرنگ (HMAC ticket)
│   ├── audit/          # AuditLog
│   ├── featureflags/   # پرچم ویژگی + کش ۱۵ث (سرویس ۱۳)
│   ├── storage/        # اشیای فایل — قرارداد S3-مانند، آداپتر FS (۱۱)
│   ├── scheduler/      # کارهای دوره‌ای: پردازشگر Outbox + پایش سلامت (۱۲)
│   ├── ai/             # دروازه AI: گیت فلگ + تلمتری + تایم‌اوت (۱۷)
│   ├── integration/    # باس یکپارچه‌سازی — کاتالوگ کانکتورها (۱۸)
│   ├── reporting/      # فراداده گزارش‌ها (۱۶)
│   └── shared/         # db, jalali, api-client, server-helpers, LABELS
├── modules/        # پلاگین‌های کسب‌وکار — نام = کد رجیستری (ADR-008)
│   ├── office-automation/  # اتوماسیون اداری + کارتابل + پیوست + AI (HITL)
│   ├── warehouse/         # انبار: موجودی، اسناد، قطعی‌سازی، درخواست کالا
│   ├── products/  partners/  dashboard/  platform/
├── components/     # ui/ (shadcn — فقط کامپوننت‌های در استفاده؛ افزودن با CLI مجاز) + shell/ + common/
├── hooks/  store/  types/
```

**قواعد وابستگی (جهت‌دار):**
- `modules/*` فقط از `core/*` و `components/*` import می‌کند — **ماژول از ماژول دیگر import ممنوع** (ارتباط فقط از طریق Outbox/رفع توسط service سطح بالا)
- `core/*` هرگز از `modules/*` یا `components/*` import نمی‌کند
- `app/api/**/route.ts` حداکثر ~۱۰ خط: `requireCtx` + فراخوانی تابع `modules/*/service.ts` — منطق کسب‌وکار در route ممنوع
- **فراخوانی مدل زبانی فقط از `core/ai/gateway`** (`runAiJson`) — گیت فلگ + تلمتری اجباری (ADR-009)
- **ذخیره فایل فقط از `core/storage`** — هرگز مسیر مستقیم FS؛ دانلود با کنترل دسترسی دامنه‌ای
- `src/lib/utils.ts` (تابع `cn`) تنها استثنا — قرارداد shadcn، جابه‌جا نکنید
- فایل سمت سرور که Prisma/SDK لمس می‌کند باید `'server-only'` داشته باشد

## چک‌لیست قبل از هر PR/تغییر

1. `bun run check` → «قبول» (دروازه ساختار/مستندات — ADR-007)
2. `bun run lint` → صفر خطا و صفر هشدار
3. رفتار را تغییر نداده‌اید؟ (رفکتور = جابه‌جایی بدون تغییر خروجی)
4. تست مرورگر با agent-browser از طریق `http://127.0.0.1:81/` (نه 3000 مستقیم!) — مسیر طلایی مربوطه
5. `tail -20 dev.log` بدون خطای جدید
6. کامیت Conventional: `feat(inventory): …` / `fix(letters): …` / `refactor(core): …` / `docs: …`
7. پیوند به `worklog.md` با همان قالب موجود

## قرارداد نام‌گذاری

| عنصر | قاعده | نمونه |
|---|---|---|
| فایل/پوشه | kebab-case | `letter-detail-dialog.tsx` |
| کامپوننت/Type | PascalCase بدون پیشوند I | `LettersView` |
| تابع | camelCase فعل+موضوع | `postWarehouseDoc()` |
| ثابت | SCREAMING_SNAKE | `SESSION_COOKIE` |
| مدل Prisma | مفرد PascalCase | `WarehouseDoc` |
| مسیر API | جمع kebab-case | `/api/letters` |
| service ماژول | یک `service.ts` در ریشه ماژول | `modules/office-automation/service.ts` |
| سند تصمیم | `ADR-NNN-slug.md` | `ADR-004-realtime-push.md` |
| سناریو | `SC-NNN-slug.md` | `SC-002-warehouse-posting.md` |

**زبان:** کد/شناسه/فیلد DB انگلیسی — UI/کامنت/مستندات/پیام خطا فارسی. ارقام نمایشی فارسی (`faDigits`)، تاریخ جلالی.

## الگوهای تثبیت‌شده (چرخ را دوباره اختراع نکنید)

- **اعلان:** همیشه `notify()` از `core/notifications` — ثبت DB + push بلادرنگ + پوشش polling خودکار است
- **رویداد دامنه:** `emitEvent()` از `core/events` (Outbox) — نه callback مستقیم بین ماژول‌ها
- **شماره سند:** `nextDocNumber(companyId, scope)` — ترتیبی سالانه جلالی per-company
- **دامنه دید داده:** همیشه `await scopeCompanyIds(ctx)` — هلدینگ (GROUP) همه شرکت‌های عضو، غیر آن فقط شرکت فعال
- **حسابرسی:** `audit()` بعد از هر عملیات نوشتاری
- **تاریخ:** فقط `core/shared/jalali.ts` (تست‌شده با ۵ تاریخ مرجع) — کتابخانه تاریخ دیگر اضافه نکنید
- **فرم‌ها (P1-T20):** react-hook-form + zodResolver؛ اسکیمای هر فرم با سازنده‌های `core/forms/schemas` (پیام = متن دقیق سرور)؛ اجزا از `components/common/form-bits` (خطا زیر فیلد)؛ خطای «سطح فرم» اقلام روی فیلد مجازی `itemsRoot` (تداخل با خطای سطری ندارد)؛ ریست هنگام باز شدن دیالوگ با الگوی microtask (`queueMicrotask(() => reset(...))`)
- **جدول/فهرست (P1-T1/T12):** `DataGrid` از `components/common/data-grid` — هر نما فقط ستون‌ها را تعریف می‌کند (key/header/cell/sortValue/serverSortKey/align/hideOnMobile)؛ جستجوی سروری با searchValue/onSearchChange، کلاینت با searchKeys؛ ردیف بازشو با expandedContent؛ **حالت سروری**: serverPagination (pageIndex/pageSize/total/onPageChange/onPageSizeChange) + serverSort/onServerSortChange — شمارنده «از N» و صفحه‌ها از total سرور محاسبه می‌شوند
- **فهرست سرور (P1-T3):** هر API فهرست با `parseListQuery(sp, { filters, sort, defaultSort, defaultPageSize })` از `core/shared/list-query` و خروجی `listEnvelope(items, total, page, pageSize)` = `{items,total,page,pageSize,pageCount}` — لیست‌سفید فیلتر/نقشه sort در route (کلید نامعتبر = پیش‌فرض، هرگز خطا)؛ سقف pageSize=۱۰۰؛ جستجوی شماره با ارقام فارسی = parseNumericInput روی q؛ هوک کلاینت با keepPreviousData و کلید شامل همه پارامترها
- **فرم چندقلمی (P1-T22):** جمع زنده/شمار اقلام با **useWatch** (watch معمولی برای فیلدهای آرایه‌ای useFieldArray رفرش نمی‌کند — تست‌شده)؛ کپی قلم = append({...سطر فعلی, qtyM2: ''})؛ گزینه‌های زیاد (۲۰۰+) با `SearchSelect` (components/common/search-select — جستجوی normalizeFaText، ۳۰+۳۰، ↑↓/Enter؛ کلیک مجدد روی گزینه انتخابی = پاک کردن)
- **قالب شماره سند/نامه (P2-T6):** نمایش شماره همیشه با `faDocNumber(num, date?)` از `core/shared/jalali` — «۱۴۰۵/۴۲» (سال جلالی تاریخ ثبت + شماره سالانه DocCounter)؛ هرگز faDigits(number) تنها
- **ردیف جدول memo شده (P1-T35):** ردیف‌های DataGrid با React.memo + مقایسه هویت row — حین اسکرول مجازی فقط ردیف‌های تازه‌وارد رندر می‌شوند (p95 اسکرول را از ۶۷ms به ۳۳ms رساند)؛ callbackهای داخل ردیف (measureRef/toggleExpand) باید useCallback پایدار باشند وگرنه memo بی‌اثر می‌شود
- **notify پس از پاسخ (P2):** push بلادرنگ در notify از `after()` از next/server استفاده می‌کند — پرامیس fire-and-forget مستقیم با پایان پاسخ route توسط رانتایم لغو می‌شود (درس G5)؛ خارج از scope درخواست fallback به void مستقیم
- **شمارش‌های دقیق (درس G5):** هر شمارنده‌ای که از فهرست صفحه‌بندی‌شده محاسبه می‌شود سقف آن صفحه است — unreadCount/مجموع‌ها همیشه با count جداگانه از DB، نه filter روی take:N
- **اقدام مخرب (P1-T23):** هر عمل برگشت‌ناپذیر/پرپیامد (قطعی‌سازی، ابطال، رد درخواست، خاموشی ماژول، خروج همه، غیرفعال‌سازی کاربر) قبل از API از `ConfirmDialog` (components/common/confirm-dialog) می‌گذرد — متن = «چه اتفاقی می‌افتد» نه «مطمئنید؟»؛ destructive=true دکمه قرمز
- **داده سمت کلاینت (P1-T2):** همه واکشی‌ها از هوک‌های queries ماژول (TanStack Query) — هرگز useEffect+useState دستی برای داده سرور؛ کلید فقط از `core/query/keys` (qk* با companyId)؛ ابطال با QK_PREFIX؛ هوک دامنه‌ای که داده ماژول دیگر می‌خواهد خودش endpoint را صدا بزند (مرز = HTTP، کش مشترک با کلید یکسان)
- **نتیجه سرویس:** همه توابع service یک `ServiceResult` برمی‌گردانند — تعریف واحد در `core/shared/types.ts`
- **فرم تاریخ:** دیت‌پیکر `react-multi-date-picker` (جلالی) — نه input متنی
- **اعتبارسنجی هویت ایرانی:** `@persian-tools/persian-tools` — نه regex دست‌ساز
- **قابلیت تدریجی/حساس:** قبل از مسیر، `isFeatureEnabled(key)` — کلید فلگ را در seed ثبت کنید
- **پلاگین جدید:** ردیف modDefs در seed (layer/domain/targetPhase/dependsOn/menus) + پوشه `modules/<code>` با service.ts + README + SPEC — دروازه CH-18/19/20 راهنمای شماست
- **ورود/امنیت (P0-T20/T21):** منطق نرخ و لاگ ناموفق فقط در `core/auth` (login-rate-limit + logFailedLogin) — route فقط IP از `x-forwarded-for` می‌دهد؛ پیام ۴۲۹ فارسی + Retry-After
- **احراز هویت دو‌مسیره (کوکی + توکن):** نشست با کوکی httpOnly (`pos_sid`) صادر می‌شود؛ چون پنل پیش‌نمایش iframe است و مرورگر کوکی third-party را مسدود می‌کند، `login` توکن نشست را در بدنه پاسخ نیز برمی‌گرداند و کلاینت آن را در sessionStorage (`saveSessionToken` از `core/shared/api-client`) نگه می‌دارد و با هدر `x-session-token` در هر درخواست می‌فرستد؛ سرور در نبود کوکی از هدر استفاده می‌کند (`getSessionCtx`). **هر fetch جدید سمت کلاینت باید یا از api-client باشد یا هدر `readSessionToken()` را بچسباند** (نمونه: بلیت سوکت در `use-realtime.ts`)
- **محیط دمو:** بنر `DemoBanner` + تب «امنیت» (فقط ادمین) با گذرواژه‌های نمایشی و تلاش‌های ناموفق — پیش از استقرار واقعی حذف/تعویض اجباری (P1-T7)
- **سنجه‌های گیت:** فرمول‌ها فقط در `modules/dashboard/service.ts` + مستند در SPEC §۳-۱؛ گزارش جلسه از همان API زنده (scripts/gate-report.ts)
- **RTL سیستمیک (۱۴۰۵/۰۶ — بازخورد کاربر):** راست‌چینی در همه لایه‌ها الزامی است (متن/لیست/تب/جدول/چارت/دیالوگ/منو/انیمیشن). سه ریشه باگ تاریخی: (۱) **Radix بدون DirectionProvider پیش‌فرض LTR است حتی با dir="rtl" روی html** → `RtlProvider` (components/shell/rtl-provider.tsx) در layout دور همه چیز است — اگر تب‌ها/منوها چپ‌به‌راست شدند این را بررسی کنید؛ (۲) کلاس‌های فیزیکی (ml/mr/pl/pr/text-left/text-right/left-*/right-*/border-l/border-r/ml-auto) **در RTL معکوس عمل می‌کنند** → فقط ویژگی منطقی (ms/me/ps/pe/text-start/text-end/start-*/end-*/border-s/border-e/ms-auto)؛ استثنا: محتوای لاتین با dir="ltr"؛ (۳) چارت/پیشرفت/سوییچ رفتار فیزیکی دارند → دستور پخت کامل در `docs/persian/persian-stack.md` §استاندارد RTL (چارت: XAxis reversed + YAxis orientation=right + Legend wrapperStyle direction rtl؛ Progress: translateX مثبت؛ Switch: rtl:-translate-x). **دروازه CH-24 هر تخلف را می‌گیرد — هیچ کلاس فیزیکی جدیدی اضافه نکنید**
- **ایندکس و کارایی (P1-T10/T11):** هیچ ایندکسی بدون «کوئری داغ اثبات‌شده» اضافه نشود — چرخه الزامی: EXPLAIN با `bun scripts/test-indexes.ts` (مسیر جدید = تست جدید در همین فایل) → ایندکس در شمای Prisma با کامنت مسیر → بودجه زمانی در `scripts/test-perf.ts`؛ داده حجمی همیشه با `bun run seed:big` (ایده: توزیع وزنی شرکت‌ها + ~۵۵٪ در ۴۵ روز اخیر تا روند داشبورد واقعی بماند + شماره‌ها از DocCounter ادامه می‌یابند)؛ **توجه: seed:big شناسه‌های صنعتی واقعی نمی‌سازد — قبل از تحویل دمو به ذی‌نفع، seed اصلی + داده واقعی جایگزین کنید**
- **RBAC دو لایه (P1-T14/T18):** سرویس نوشتن جدید با `requireWriteRole(ctx)` شروع می‌شود (isAdmin بای‌پس؛ VIEWER/نامعلوم → 403 فارسی) و endpoint مدیریتی با `requireSettingsAdmin(ctx)` — هر دو در `core/tenancy/tenancy.ts`؛ آینه کلاینت = هوک `useCanWrite` (hooks/use-can-write) که دکمه‌های ثبت را مخفی می‌کند (مخفی‌سازی UX است، نه گارد — سرور حاکم است)؛ داده کاربران برای غیرمدیر فقط دایرکتوری حداقلی (id/fullName/jobTitle) — نام کاربری/ماتریس/وضعیت داده حساس مدیریتی است؛ تست متقاطع هر تغییر RBAC = سنجه جدید در `scripts/test-rbac.ts` (اکنون ۱۰۳ سنجه)
- **گارد API سطح ماژول (P1-T28):** هر route ماژول عملیاتی جدید با `requireModule('<code>')` از `core/shared/server-helpers` شروع می‌شود (نه requireCtx خالی) — نگاشت code: office-automation / warehouse / products / partners؛ ماژول خاموش سراسری/شرکتی = 404 فارسی، بدون بای‌پس حتی برای مدیر پلتفرم (آینه منطق منوی view-registry)؛ رجیستری ماژول‌ها و APIهای پلتفرمی (auth/notifications/audit/dashboard/users) هرگز گارد ماژول نمی‌گیرند (مسیر بازگشت و زیرساخت مشترک). **⚠️ درس dev:** کش درون‌فرایندی state مشترک بین routeها باید سینگلتون globalThis باشد (`globalThis.__ioXxx`) — در dev هر route نمونه جدا از ماژول دارد و invalidate درون‌ماژولی فقط کش همان route را پاک می‌کند (باگ کشف‌شده P1-T28)
- **تنظیمات per-company (P1-T29/T30):** تنظیم اختصاصی شرکت جدید = کلید در سفیدلیست `COMPANY_SETTING_VALIDATORS` (سرویس platform) + کلید/پیش‌فرض در `getCompanySettings` خوانده‌شده + هسته خواندن `core/tenancy/company-settings.ts` + ردیف UI در تب «شرکت» settings-view؛ هر تغییر با audit COMPANY_SETTING ثبت می‌شود؛ مدل CompanySetting (کلید-مقدار، @@unique(companyId,key)) — هرگز فیلد ستونی مستقیم روی Company اضافه نکنید
- **خروجی اکسل فارسی (P1-T15):** هر CSV آینده باید از الگوی `exportAuditCsv` پیروی کند — بایت اول فایل UTF-8 BOM (بدون آن اکسل ویندوز فارسی را مربع‌مربع نشان می‌دهد) + CRLF بین سطرها + رد کردن سلول‌های حاوی کاما/نقل‌قول/خط جدید با نقل‌قول دوتایی + Content-Disposition با نام ASCII امن + متادیتای سطر در هدر سفارشی (X-Csv-Rows) برای بازخورد toast؛ تست BOM با **بایت خام** (arrayBuffer) انجام می‌شود نه text() — چون TextDecoder هنگام دیکد BOM را حذف می‌کند (دروغ‌آشکار تست!)
- **پوسته چندسندی (P1.5):** ناوبری هیچ‌وقت با state محلی نما انجام نمی‌شود — `useWorkspace` (store/workspace): `openView(viewKey)` برای فهرست‌ها، `openRecord(viewKey, id, title?)` برای رکوردها (dedup — بازِ مجدد تب موجود را فعال می‌کند)، `openNew(viewKey)` برای فرم ثبت (فقط یکی per نما). پس از ذخیره فرم، تب با `materializeTab(tabId, recordId, title)` به تب رکورد تبدیل می‌شود (انباشت تب «جدید» ممنوع). رندر نما فقط از `view-registry.tsx` (LIST_VIEWS/RECORD_VIEWS) — نمای جدید = ثبت در این دو شیء + CH-18 خودش پیوند seed را می‌گیرد؛ AppShell فقط چیدمان است. سوییچ شرکت/خروج = `resetWorkspace()` (داده تب‌ها شرکت‌محور است).
- **صفحه رکورد ERP (P1.5):** نمایش/ویرایش رکورد **صفحه کامل است نه دیالوگ** (بازخورد کاربر) — همیشه از `RecordPageShell` (components/common/record-page-shell) ساخته می‌شود: بردکرامب بازگشت (useRecordNav) + هدر (آیکون/عنوان/نشان‌ها) + نوار وضعیت چرخه عمر (steps+currentIndex؛ وضعیت‌های شکست مثل CANCELLED/REJECTED با statusError خارج از مسیر) + شناسنامه InfoGrid + نوار اقدام + تب‌های داخلی + فوتر چسبان فرم‌ها. دیالوگ فقط برای تأیید (ConfirmDialog) و ویزارد کوتاه مجاز است. داده رکورد با هوک per-record (مثل useLetterQuery) + `setTabTitle` پس از بارگذاری عنوان تب را دقیق می‌کند.

- **سکشن‌بندی فرم ERP (P2.5-U1، الگوی D365 Section/Odoo Group):** هر فرم ثبت/ویرایش از کامپوننت `FormSection` (components/common/form-section) ساخته می‌شود: عنوان گروه + توضیح یک‌خطی + بدنه گرید (`cols` = ۲ پیش‌فرض / ۳ در lg+ / `"free"` برای جدول اقلام و ماتریس) + `collapsible` فقط برای سکشن‌های اختیاری. فرم‌ها دیگر grid خالی «شناور» ندارند — هر گروه فیلد سکشن عنوان‌دار دارد. متن بلند فرم (متن نامه) با `bodyClassName="max-w-4xl"` محدود می‌شود تا خط‌ها در عرض کامل خوانا بمانند.
- **عرض کامل پوسته (P2.5-U1):** محتوای پوسته سقف عرض ندارد (`w-full` در app-shell) — مانیتورهای دفتری ۱۵۶۰+ تمام‌عرض استفاده می‌شوند؛ کنترل خوانایی به‌جای سقف سراسری، در سطح بدنه‌های متنی/گرید انجام می‌شود. InfoGrid شناسنامه تا ۶ ستون در xl+ می‌شکند.
- **انتخاب گروهی و اقدام گروهی (P2.5-U2، الگوی bulk actions در D365/Gmail):** گریدها با prop کنترل‌شده `bulkSelection` روی DataGrid (selectedIds/onSelectedIdsChange/isRowSelectable/disabled/rowAriaLabel) — مالکیت state نزد نما است تا پاک‌سازی پس از عملیات همان‌جا باشد؛ «انتخاب همه» فقط سطرهای همین صفحه را می‌گیرد (صفحه‌بندی سروری). نوار اقدام گروهی همیشه از `BulkActionBar` (components/common/bulk-action-bar) — شناور پایین، شمار فارسی + دکمه‌های اقدام + لغو؛ اقدام اصلی از ConfirmDialog می‌گذرد و نتیجه سرور با `toastBulkResult({affected, results, unit, actionTitle, doneVerb})` جمع‌بندی می‌شود: **ردِ هر رکورد با شماره و دلیل اعلام می‌شود، سکوت ممنوع**. سرویس گروهی جدید = حلقه روی همان سرویس تک‌رکورد (گارد و اثر و حسابرسی عیناً همان؛ بدون امتیاز جدید) + سقف ۱۰۰ + گارد نقش پیشگیرانه یک‌باره (نمونه: bulkArchiveLetters · bulkDecideRequests)؛ `isRowSelectable` کلاینت باید آینه دقیق گارد سروری باشد وگرنه UI چیزی را انتخاب‌پذیر نشان می‌دهد که سرور رد می‌کند. مسیر API گروهی = سگمنت استاتیک `…/bulk` بر داینامیک `[id]` اولویت دارد (قرارداد Next.js App Router).
- **ذخیره خودکار پیش‌نویس (P1-T24):** هر فرم ثبت بزرگ (چند فیلد/چندقلمی) از `hooks/use-draft` استفاده می‌کند: `useDraftRestore(viewKey, companyId, defaults)` مقادیر اولیه merge‌شده با پیش‌نویس localStorage برمی‌گرداند (merge سطح‌اول = مقاوم به تغییر اسکیما)، `<DraftAutosave control storageKey onSaved/>` کامپوننت جدا با debounce ۷۰۰ms فقط وقتی فرم dirty است (والد رندر نمی‌شود)، و `clearDraft` در سه نقطه: ثبت موفق، انصراف صریح، دکمه «دورریختن». **بستن تب (× یا Esc) پیش‌نویس را پاک نمی‌کند** — کاربر تصادفی تب را بسته و برگشته. کلید = `io.draft.v1:<companyId>:<viewKey>` (شرکت‌محور). defaults هر فرم در تابع `xxxDefaults()` جدا از کامپوننت.
- **پالت فرمان و میان‌برها (P1-T25/T27):** ناوبری سریع فقط از `components/shell/command-palette.tsx` — منبع نماها = منوی پلاگین‌ها با همان گاردهای سایدبار؛ رکورد قابل جستجو جدید = افزودن به items با گروه مناسب (نامه‌ها سروری q≥۲، بقیه فیلتر کلاینت روی کش مشترک). میان‌برهای سراسری فقط در `keyboard-shortcuts.tsx` رجیستر می‌شوند — **تشخیص کلید با e.code (فیزیکی، مستقل از چیدمان فارسی)** و «؟» سه‌راهی (e.key فارسی/انگلیسی + Shift+Slash). اکشن جدید داخل فرم‌ها: Ctrl+Enter = submit (در فرم سند: پیش‌نویس، Ctrl+Shift+Enter = قطعی‌سازی).
- **توست یکدست (P1-T34):** فراخوانی خام `toast({...})` ممنوع — فقط `toastOk({title, description})` / `toastErr({title='خطا', description})` / `toastInfo({...})` از `hooks/use-toast`؛ قالب: عنوان = عملیات («سند قطعی شد»)، شرح = پیامد/شماره؛ دیرش خودکار per-type (۴/۷/۵ ثانیه). متن عنوان همیشه string ساده (تداخل تایپ Radix با ReactNode).
- **حالت خالی/بارگذاری واحد (P1-T33):** هیچ «متن لخت» یا صفحه سفید — `EmptyState` (icon/title/hint/action/compact) و `LoadingState` (label/rows) از `components/common/ui-bits`؛ DataGrid با `emptyHint`/`emptyAction` و ورودی جستجویش `data-grid-search` دارد (میان‌بر «/» همان را فوکوس می‌کند).
- **فیلتر فیلدهای اختیاریِ پاسخ دوسطحی (بررسی عمیق ۱۴۰۵/۰۶):** `listUsers` برای مدیر پروفایل کامل و برای غیرمدیر دایرکتوری حداقلی (id/fullName/jobTitle — بدون isActive) برمی‌گرداند؛ پس فیلتر کلاینت روی چنین فیلدهایی همیشه با الگوی مقاوم `!== false`/`!= null` انجام می‌شود نه truthiness — `.filter(u => u.isActive)` کل فهرست را برای غیرمدیران خالی می‌کند (باک G5: فهرست «ارجاع اولیه» نامه برای دبیرخانه خالی بود).
- **منطق مالکیت تک‌منبعی (بررسی عمیق ۱۴۰۵/۰۶):** شرط‌های مالکیت/اقدام (isMine و مانند آن) وقتی در دو لایه لازم می‌شوند (دکمه در صفحه، پنل در تب داخلی) باید **عیناً آینه** هم باشند یا در یک تابع مشترک بیایند — واگرایی آنها یعنی دکمه‌ای که پنلی را باز می‌کند که هرگز رندر نمی‌شود (باگ G2: پنل ارجاع نامه DRAFT خودساخته).
- **اجزای مشترک موجد فرم (بررسی عمیق فرم‌ها ۱۴۰۵/۰۶):** قبل از ساختن مؤلفه جدید، این‌ها از `form-bits` موجودند: `CharCount` (شمارنده n/max زیر فیلد — از prop `extra` روی FieldInput/FieldTextarea که حتی هنگام خطا هم رندر می‌شود)، `PasswordInput` (eye toggle — propهای onKeyUp/onKeyDown برای CapsLock/Enter)، `PasswordStrength` + `passwordScore` (نوار ۴ قسمتی، فقط نمایش — سیاست حداقلی جداگانه اعمال می‌شود)، `KbdHint` (تراشه Ctrl+Enter در فوتر)، `RowWarning` (هشدار کهربایی سطری غیرمسدودکننده — متمایز از RowError قرمز)؛ عدد اختیاری اسکیما با `faOptionalNumber(label)` از `core/forms/schemas` (خالی=۰ مجاز، مقدار نامعتبر/منفی = خطای زیر فیلد — هرگز `z.string()` لخت برای فیلد عددی)
- **موجودی زنده در فرم‌های اقلام (بررسی عمیق فرم‌ها):** نمایش موجودی کالای انتخابی در فرم سند/درخواست با `useProductStockQuery(warehouseId, productId)` از `modules/warehouse/queries` (فیلتر productId سمت سرور، پاسخ کوچک و کش‌شده) — همیشه «کل کالا + واریانت (تون/کالیبر/درجه)» و فقط **هشدار** بیش‌برداشت/کمبود؛ تصمیم نهایی و رد نهایی با سرور است (آینه منطق postDoc) — کلاینت گارد نیست
- **dirty tracking و گارد بستن تب (بررسی عمیق فرم‌ها):** فرم‌های دستی (بدون RHF) با snapshot مقادیر بارگذاری‌شده در `useRef` مقایسه می‌شوند (`editDirty`)؛ دکمه «ذخیره تغییرات» بدون تغییر غیرفعال + برچسب «تغییری ثبت نشده است»؛ دکمه «انصراف و بستن تب» وقتی dirty است از ConfirmDialog «بستن بدون ذخیره؟» می‌گذرد. فرم‌های RHF همان را از `formState.isDirty` می‌گیرند (نمونه: فرم نامه — انصراف dirty = تأیید حذف پیش‌نویس، چون انصراف صریح = clearDraft)
- **پیش‌نویس فرم دستی (بررسی عمیق فرم‌ها):** فرم‌های چندفیلدی بدون RHF (فرم ایجاد کاربر) از همان قرارداد `io.draft.v1` با `readDraft/writeDraft` مستقیم + debounce دستی ۷۰۰ms استفاده می‌کنند (نه DraftAutosave که control می‌خواهد)؛ بازنویسی مقدار پیش‌فرض پس از واکشی (مثل عضویت اولیه) فقط وقتی انجام می‌شود که فرم خالی است (`fMemberships.length === 0`) وگرنه پیش‌نویس بازیابی‌شده له می‌شود
- **شخصی‌سازی ماندگار (P2.5-U3، الگوی D365 personal views / Odoo favorites — قرارداد io.ui.v1):** ترجیح نمایشی per کاربر همیشه از `core/shared/ui-prefs` با کلید `io.ui.v1:<userId>:<name>` — نه localStorage خام. قواعد اجباری: (۱) خواندن **در lazy initializer** useState (الگوی use-draft)، هرگز در useEffect با setState همگام (دروازه lint react-hooks/set-state-in-effect) — این زیردرخت‌ها فقط پس از احراز هویت mount می‌شوند پس SSR همگان است؛ (۲) فقط «انحراف از پیش‌فرض» ذخیره می‌شود (مخفی‌های ستون، نه کل نقشه — ستون تازه نسخه بعد همیشه نمایان)؛ (۳) هر pref دکمه بازگشت به پیش‌فرض دارد (ستون‌ها: آیتم «بازنشانی به پیش‌فرض»). مصرف‌کنندگان: DataGrid با `persistKey` (چیدمان ستون per نما) · FormSection با `persistKey` (باز/بسته سکشن‌های اختیاری) · پین نما (سایدبار؛ فقط viewKey ذخیره می‌شود تا برچسب/آیکون از رجیستری زنده بخواند و پین نمای خارج از دسترس رندر نشود) · بازه داشبورد. مهاجرت به نمای ذخیره‌شده سروری در P6 ثبت شده — قرارداد UI ثابت می‌ماند.
- **داشبورد نقش‌محور (P2.5-U8، الگوی Fiori Launchpad / D365 dashboards):** KPI = کارتِ اقدام است — هر StatCard عملیاتی `onClick` + `hint` می‌گیرد (کلیک → `openView` نمای همان سنجه؛ hover نشانگر + aria-label مقصد). بلوک‌های حاکمیتی/راهبری داشبورد (گیت، کاتالوگ پلاگین، سنجه AI) همیشه پشت گارد `isAdmin || activeCompany.role === 'ADMIN'` (هم‌قاعده «تنظیمات/کاربران») — داده حساسی پشتش نیست، رندر نقش‌محور صرفاً تفکیک مخاطب است. فید فعالیت همیشه با فیلتر سروری نویز نشست (`action notIn [LOGIN, LOGIN_FAILED, LOGOUT, LOGIN_NEW_DEVICE, SWITCH_COMPANY]` + take بزرگ‌تر → slice) — فید پر از «ورود به سامانه» فید مرده است. تعداد KPI کارت‌ها با گرید هم‌ضرب است (۴ کارت در گرید ۴ستونه — ردیف یتیم ممنوع)
- **پیش‌نمایش کنار فهرست — Master-Detail (P2.5-U4، الگوی Fiori FCL / SF Split View):** فهرست‌های رکوردی در دسکتاپ (lg+) با prop `preview` روی DataGrid ({selectedId, onPreview, onClose}) — کلیک ردیف = پیش‌نمایش پنل کناری (نه تب جدید)؛ «باز کردن کامل» = همان `openRecord` قبلی. قواعد اجباری: (۱) پنل **فقط-خواندنی** است — اقدام/ویرایش همیشه در صفحه رکورد (تصمیم §۳ P2.5)؛ (۲) در موبایل (<lg) رفتار کلیک = onRowClick قبلی می‌ماند — تشخیص با `useIsDesktop` (useSyncExternalStore روی matchMedia، نه setState در effect)؛ (۳) کیبورد: ↑↓ فوکوس ردیف با roving-tabindex (فقط ردیف فعال tabIndex=0) + پیش‌نمایش زنده وقتی پنل باز است، Space/Enter = پیش‌نمایش، Esc روی ردیف = بستن پنل با stopPropagation تا میان‌بر سراسری «بستن تب» نسوزد؛ (۴) state پنل از `usePreviewPanel(viewKey)` (قرارداد io.ui.v1 `pv:<viewKey>` = {open, width} — عرض با دستگیره کشیدنی ۳۲۰–۵۶۰ و ثبت debounceشده)؛ (۵) محتوای پنل از همان کوئری صفحه رکورد می‌آید (کش مشترک — الگوی LetterPreviewContent/WhDocPreviewContent/RequestPreviewContent)؛ (۶) callbackهای کیبورد/کلیک ردیف در DataGrid همیشه از ref تازه می‌خوانند (الگوی bulkRef) — ردیف‌های memo شده closure کهنه نبینند. کارت‌های فهرست کارتی (درخواست‌ها) همین الگو را با onPreview روی کارت می‌گیرند.
- **خروجی داده per-view (P2.5-U6، تعمیم الگوی P1-T15):** هر نمای فهرست دکمه «خروجی اکسل» دارد که **همان where/فیلترهای فعال فهرست** را می‌گیرد — نه endpoint جدا با فیلتر خودش (واگرایی فیلتر = خروجی‌ای که با آنچه کاربر می‌بیند فرق دارد). قواعد: (۱) زیرساخت `core/shared/csv` — buildCsvDocument (BOM سه‌بایتی + CRLF + escape فقط-در-صورت-لزوم + سقف ۵٬۰۰۰ + پرچم capped) — هرگز CSV دستی در سرویس ساخته نشود؛ (۲) route همان GET فهرست با `?format=csv` می‌ماند (نه route جدید) + هدرهای X-Csv-Rows/X-Csv-Capped + Content-Disposition نام‌دار؛ (۳) کلاینت = هوک `use-csv-export` (apiDownload + توست شمار ردیف/سقف) — path کامل API پاس شود (SPA روی / است؛ کوئری نسبی به صفحه می‌رود نه API!)؛ (۴) دکمه خروجی **برای همه نقش‌هاست** (خواندن است) — فقط دکمه‌های نوشتن پشت canWrite؛ (۵) تست BOM با **بایت خام** (arrayBuffer) نه text().
- **خط زمان رکورد (P2.5-U5، الگوی Odoo Chatter / D365 Timeline):** تاریخچه اقدامات هر رکورد از سجل حسابرسی می‌آید، نه جدول اختصاصی — سرویس `entityTimeline` در `core/audit/audit` با **لیست‌سفید نهادها** (TIMELINE_ENTITIES — نهاد جدید = یک ردیف) + گارد `scopeCompanyIds` (کاربر فقط سجل رکوردهای شرکت‌های خودش). این مسیر برای همه نقش‌هاست (برخلاف listAudit مدیریت‌محور) چون خروجی همان رکوردی است که دیدنش مجاز است. قواعد: (۱) API `GET /api/audit/timeline?entity=&id=` زیرساخت هسته است (بدون requireModule)؛ (۲) کلاینت = کامپوننت مشترک `RecordTimeline` (components/common/record-timeline) + هوک `useRecordTimelineQuery` (qkTimeline/QK_PREFIX.timeline؛ staleTime 0 — نمایان‌شدن فوری اقدام مهم‌تر از کش)؛ (۳) مصرف از طریق `innerTabs` صفحه رکورد (RecordPageShell) با برچسب شمارنده‌دار «اقلام (N)»؛ (۴) پس از هر اقدام نوشتاری، مصرف‌کننده `invalidateQueries({queryKey: QK_PREFIX.timeline})`؛ (۵) برچسب اقدام‌های جدید در ACTION_FA (audit-labels مشترک سرور/کلاینت) — اقدام بدون برچسب خام نمایش داده می‌شود؛ (۶) نامه عمداً مصرف نمی‌کند (گردش اختصاصی خودش دارد — تکرار اطلاعات است).
- **پنل رکورد نیم‌صفحه — FCL نسل ۲ (P2.5-U9، الگوی Fiori Flexible Column Layout):** باز کردن رکورد از فهرست در دسکتاپ = «یک کد، دو قاب» — همان کامپوننت صفحه رکورد (رجیستری `RECORD_VIEWS` در `components/shell/record-views.tsx`) هم در تب رکورد و هم داخل پنل کنار فهرست رندر می‌شود؛ نما هیچ نسخه دومی از صفحه رکورد نمی‌سازد. قواعد اجباری: (۱) نما state پنل را با `usePreviewPanel(viewKey)` نگه می‌دارد و به `FclRecordPane` (components/shell/fcl-record-pane) می‌دهد — mode ∈ {'narrow','half'} با **پیش‌فرض half** (خواسته کارفرما «تقریباً نصف»)، ترجیح `pv:<viewKey>` = {open, width, mode} در io.ui.v1؛ (۲) در حالت half عرض = ۵۰٪ (minWidth 420) و **دستگیره درگ فقط در narrow** (۳۲۰–۵۶۰)؛ (۳) صفحه رکوردِ داخل قاب از زمینه `EmbeddedRecordProvider`/`useIsEmbeddedRecord` (record-page-shell) می‌فهمد جاسازی است → بردکرامب/بازگشت موبایل/«بستن تب» مخفی — prop-drilling ممنوع؛ (۴) تب ساختگی `fcl:<view>:<id>` هرگز در store ثبت نمی‌شود (setTabTitle بی‌اثر می‌ماند) و `key={recordId}` = ریمانت تازه per رکورد؛ (۵) فرم «جدید» هرگز در پنل باز نمی‌شود (فاز ۲ صریح)؛ (۶) میان‌برهای گرید در حالت پنل: Space/Enter = پیش‌نمایش، **Ctrl+Enter = تمام‌صفحه** (preview.onOpenFull)، Esc = بستن پنل با `preview.paneOpen` در گارد (نه فقط selectedId — درس زیر)؛ (۷) «تمام‌صفحه» = همان openRecord قبلی؛ موبایل (<lg) رفتار قبلی دست‌نخورده.
- **لایه‌بندی Esc (درس U9):** میان‌بر سراسری Esc (بستن تب فعال) و Esc محلی (بستن پنل/دیالوگ) باید **از لایه بالایی به پایین** مصرف شوند — گارد محلی علاوه بر «رکوردی انتخاب شده» باید «پنل باز است» را هم بپوشاند (`paneOpen`)، وگرنه وضعیت بازیابی‌شده پس از reload (پنل باز، بدون رکورد) Esc را به میان‌بر سراسری می‌فرستد و **تب فعال بی‌دلیل بسته می‌شود**. هر عنصر شناور جدید (پنل/پاپ‌آور/سایدبار) باید Esc خودش را با stopPropagation پیش از window مصرف کند.
- **تب داخلی رکورد استاندارد (P2.5-U10، الگوی D365 entity tabs):** هر صفحه رکورد تب‌های داخلی‌اش را از هوک مشترک `useRecordInnerTab(viewKey, recordId, tabs)` می‌گیرد — مقدار اولیه: ?t آدرس (لینک مستقیم) → ترجیح `it:<viewKey>` (آخرین تب همان رکورد، سقف ۱۰۰) → تب اول؛ تغییر تب = ثبت ترجیح + replaceState ?t. قالب و ترتیب ثابت: «محتوا/اقلام (N) → گردش/خط زمان → پیوست‌ها (N)» با شمارنده فارسی؛ شناسنامه همیشه در هدر ثابت است (تب نیست). آدرس‌پذیری تب فعال از store می‌آید: `syncUrl` در commit قیف واحد (?view / ?rec=<view>:<id>)؛ openRecord/openNew = pushState (Back مرورگر = تب قبلی با popstate handler)؛ ?t فقط مال رکورد فعال است و همگام با تب داخلی replace می‌شود؛ boot با ?rec/?view بر sessionStorage مقدم است و وضعیتش persist می‌شود (رفرش لینک مستقیم کار می‌کند).
- **جستجوی تمام‌متن خارج Prisma (P2-T5 — الگوی letter_fts):** جستجوی واژه‌محور دامنه‌ای = جدول letter_fts «خارج Prisma» (Postgres: ستون tsvector تولیدی + ایندکس GIN — پورت لایه سوم ۱۴۰۵/۰۶/۱۰) (شِما صاحب جداول خودش) + ماژول خودترمیم `fts.ts`/`fts-sql.ts` مشترک سرور/seed. **`prisma migrate/db push` جدول مشتق‌شده را DROP می‌کند** — اشکال نیست: بلافاصله بعدش طبق بند ۱۴ سرور احیا می‌شود و قلاب بوت ensureLetterFts ایندکس را بازمی‌سازد (اولین جستجو هم ensure دارد)؛ چک CH-25 جداول letter_fts* را از دریفت مستثنا کرده است. قواعد الزامی: (۱) همه SQL از `$queryRawUnsafe` خود Prisma — سرور Next روی **Node** اجرا می‌شود (زنجیره bun→node) و bun:sqlite در src ممنوع؛ پورت Postgres: پارامترهای $n + to_tsquery('simple') تزریق‌ناپذیر (درس‌های ۳۷–۴۱). (۲) ایندکس و پرس‌وجو هر دو با `normalizeFaText` نرمال می‌شوند (ك/ي عربی، ارقام، ZWNJ→فاصله)؛ توکن‌ها فقط الفبایی/رقمی‌اند → رشته MATCH تولیدی هرگز نحو FTS ندارد (تزریق‌ناپذیر؛ lowercase → AND/OR عملگر نمی‌شوند). (۳) توکن ≥۲ نویسه؛ حرف با پیشوند `*`، رقم **دقیق** بدون *. (۴) عقب‌گرد contains در نبود توکن معتبر/شکست ensure — جستجو هرگز نمی‌شکند. (۵) خودترمیم شمارشی: هر جستجو دو COUNT ارزان؛ ناهماهنگی → rebuild (~۱–۲ث روی ۱۰هزار نامه). (۶) سینک سطح اپ: قلاب upsert در createLetter (ستون‌های ایندکسی فقط هنگام ثبت تغییر می‌کنند) + rebuild در seed + hydration با IN کوچک **بدون orderBy** (ترتیب از SQL — گسترش درس ۲۳). (۷) ماژول مشترک سرور/seed نباید server-only داشته باشد (در bun خام throw می‌کند) و نباید بسته Prisma را import کند (CH-02) → کلاینت ساختاری `FtsDbClient` در fts-sql.ts.
- **گارد بستن تب کثیف (P2.5-U10 — #۴/#۲۶ پژوهش ۰۲):** همه مسیرهای بستن تب (× تب، کلیک‌وسط، Esc سراسری) فقط از `useDirty.requestClose(tabId)` (store/dirty) می‌گذرند — کثیف → DirtyCloseGuard (ConfirmDialog مخرب «دور انداختن تغییرات»)؛ تمیز → بستن فوری. فرم‌ها با `useDirtyTracking(tabId, isDirty, label)` ثبت می‌کنند (RHF: formState.isDirty؛ فرم دستی: editDirty؛ فایل‌های در انتظار هم سیگنال‌اند) و نقطه کهربایی `data-dirty-dot` روی تب می‌نشیند. پیش‌نویس خودکار مستقل است — گارد هشدار UX است نه مسیر داده.
- **پورتال چاپ / نمای A4 (P2.5-U7 / P2-T7 — الگوی «یک کد، دو قاب» چاپ):** چاپ هر سند Business (نامه و آینده: سند انبار/قرارداد) = کامپوننت پورتالی به `document.body` با ریشه `.letter-print-root` + کاغذ `.letter-print-sheet` (۲۱۰×۲۹۷ میلی‌متر + پدینگ داخلی)؛ CSS حاکم در `globals.css` (نمایش = اورلی تیره/اسکرول، چاپ = `@page A4 margin:12mm` + `body > :not(.letter-print-root) {display:none}` + عرض کاغذ auto تا در ناحیه قابل‌چاپ هر پرینتر جا شود + `.no-print` برای نوار ابزار). سه قاعده الزامی: (۱) ریشه پورتال **`data-state="open"`** بگیرد تا گارد Esc سراسری (anyOverlayOpen) آن را پنجره حساب کند — بدون آن Esc هم پورتال را می‌بندد و هم تب فعال را (درس ۲۲)؛ (۲) سربرگ/پاورقی per-company از `CompanySetting` با کلیدهای `letterhead.subtitle/footer` (getLetterhead در core/tenancy؛ سرویس جزئیات از **شرکت خودِ رکورد** می‌خواند نه شرکت فعال ناظر) و پیش‌فرض متن = نام قانونی شرکت؛ (۳) اقدام فقط-خواندنی چاپ هم سجل حسابرسی دارد — شاخه `PRINT` در actOnLetter قبل از گارد دارنده/وضعیت (بایگانی‌شده هم چاپ می‌شود) و کلاینت آن را fire-and-forget می‌فرستد (شکست شبکه چاپ را نمی‌شکند).

- **کار زمان‌بند ماژولی (P2-T11):** کار دوره‌ای جدید = runner خالص در `core/scheduler/jobs/<key>.ts` (فقط core/shared و core/notifications وارد می‌کند — بدون حلقه import به scheduler) + ثبت استاتی در خود scheduler (`registerJobRunner` — رجیستری روی globalThis تا نمونه‌های HMR و حلقه زنده همه یک Map را ببینند) + ردیف idempotent `ScheduledJob` با اسکریپت `scripts/add-<key>-job.ts` (الگوی add-session-purger) + در صورت نیاز اجرای دستی: `runJobOnce(key)` از طریق `POST /api/platform/jobs/run` (گارد requireAdmin + سجل JOB_RUN) — مسیر تست «رویداد با تاریخ ساختگی» و اجرای فوری عملیاتی.
- **عدم اسپم اعلان دوره‌ای (P2-T11):** هر اعلانی که «باید فقط یک‌بار» فرستاده شود `dedupKey` یکتا دارد (`Notification.dedupKey @unique`) — کلید از **منبع واقعی رویداد** ساخته می‌شود (`deadline-reminder:<letterId>:<ref|letter>:<نقطه>`) تا هم پایدار باشد هم معنادار؛ `notify()` روی P2002 بی‌صدا null برمی‌گرداند (تکرار حتی با اجرای هم‌زمان در سطح DB رد می‌شود) و push بلادرنگ فقط برای رکورد تازه می‌رود.
- **مهلت مؤثر نامه (P2-T10 — گام ?? نامه):** همه نشان‌های مهلت (شناسنامه/کارتابل/پیش‌نمایش/یادآور) از یک تعریف می‌آیند: گام جاری = آخرین ارجاعِ رساننده نامه به دارنده فعلی؛ مهلت مؤثر = مهلت گام اگر داشت وگرنه مهلت نامه. کمکی‌های مشترک `modules/office-automation/deadline.ts` (deadlineTone قرمز/کهربایی/عادی + liveStepDeadline) — کلاینت و runner ساعتی (سرور) هر دو همین معنا را پیاده می‌کنند؛ واگرایی = یادآوری برای مهلت اشتباه.

- **گزارش مدیریتی بازه‌ای (P2-T13):** گزارش تجمیعی جدید = سرویس با پارامترهای صریح (from/to جلالی + preset این‌هفته/هفته‌گذشته + staleDays) و گارد `requireSettingsAdmin`؛ منبع داده = system-of-recordِ حوزه (LetterReferral برای گردش — نه AuditLog که پیوندهای تاریخی ناقص دارد)؛ خروجی هم JSON برای UI و هم `?format=md` برای متن قابل رونوشت/ذخیره (نام فایل ASCII — هدر Latin-1 رقم فارسی نمی‌پذیرد)؛ UI = کارت در تنظیمات←یکپارچه‌سازی + پورتال چاپ `report-print-root` (ساده‌تر از الگوی U7: بدون پیش‌نمایش — در نمایش display:none، فقط حین چاپ رندر و afterprint برداشت). preset=last = شنبه پیشین تا جمعه پیشین (شنبه جاری −۱ روز، نه +۶ — اشتباه رایج ۱۴روز می‌سازد).
- **ثابت مشترک سرور/کلاینت (P2-T14):** فهرست/سقفی که کلاینت هم باید بداند (گزینه‌های Select و آستانه اعتبارسنجی) → فایل client-safe جدا در `modules/<mod>/<چیز>.ts` (الگوی deadline.ts و ai-categories.ts)؛ هرگز از service.ts (server-only) export نشود — import کلاینت خطای build/زمان اجرا می‌دهد و تکرار تعریف = واگرایی گارد.
- **کارت پیشنهاد ویرایش‌پذیر HITL (P2-T14):** الگوی «پیشنهاد + پیش‌نویس موازی»: state اصلی suggestion (تحت تأثیر کاربر نیست) + state پیش‌نویس draft؛ ویرایش‌شدگی = مقایسه draft با suggestion (دکمه بازگردانی فقط وقتی متفاوت)؛ اعمال = مقادیر draft؛ **سجل = مقادیر نهایی اعمال‌شده** (نه پیشنهاد خام) — بازسازی تاریخچه از حسابرسی ممکن است.
- **OCR اسکن فارسی (P2-T16):** استخراج متن از تصویر = همیشه ماژول بی‌حالت `modules/office-automation/ocr.ts` (spawn آرایه‌ای tesseract `-l fas --psm 6`؛ stdin→stdout بدون فایل موقت؛ جادوی بایت + تایم‌اوت SIGKILL)؛ ساختاردهی فقط از runAiJson؛ **قواعد پرامپت را در کد هم تضمین کن** (حذف مکانیکی سطرهای سربرگ از body — مدل ناکاتشی است)؛ HITL = merge نه replace (متن فقط در فیلد خالی)؛ سیاست «سری» با توکن‌سازی کلمه‌ای نه includes خام («سریع» بی‌گناه است)؛ تست دقت OCR = نمونه‌های رندرشده با مرورگر (شکل‌دهی RTL واقعی) + سنجه توکن‌محور نرمال‌شده (نیم‌فاصله/فاصله/دونقطه حذف شوند وگرنه «صورت‌حساب شماره ۵۱۲۳» بی‌گناه شکست می‌خورد)

- **کدگذاری ساختارمند «کد به‌عنوان جمله» (P4-T0):** ساخت/رمزگشایی کد هر خانواده قلم فقط از موتور `core/coding` — طرحواره‌ها داده‌اند (CodeScheme/CodeSegment/CodeEnumValue؛ seed: `scripts/seed-code-schemes.ts` idempotent). قواعد: (۱) کد همیشه لاتین ذخیره می‌شود — ورودی کاربر با `digitsToLatin` نرمال؛ (۲) شمارنده‌ها (مثل کد طرح) از DocCounter موجود با scope `CODE:<scheme>:<key>` می‌آیند — مصرف دائمی است ⇒ صدور = requireWriteRole + سجل CODE_COMPOSE؛ (۳) «کد مادر» = n جزء ابتدایی (motherSegments — تبصره سند شرکت)؛ (۴) پیش‌نمایش ساخت زنده در کلاینت ولی حکم/شمارنده با سرور؛ (۵) کامپوننت مشترک `CodeComposer` (components/common — family prop طرحواره پیش‌فرض را برمی‌گزیند) + نگاشت معنایی mapsTo برای پیش‌پرکردن فیلدهای مستردیتا؛ (۶) کدینگ زیرساخت هسته است (بدون گارد ماژول) و طرحواره اختصاصی شرکت override سراسریِ همان code می‌کند؛ (۷) افزودن خانواده جدید (تجهیزات/مواد اولیه/…) = ردیف seed، نه کد.

## درس‌های تست/محیط U10 (تکمیلی)

- **URL با پارامتر در ab باید کوتیشن تک بگیرد**: `ab("open 'http://…/?a=1&b=2'")` — علامت & در شل، دستور را جدا می‌کند و پارامترهای بعدی بی‌صدا گم می‌شوند (ab از execSync شلی استفاده می‌کند)؛ علامت «همه‌چیز سبز به‌جز پارامتر دوم» = همین.
- **تایپ مصنوعی در ورودی React**: مقداردهی `el.value=…` + dispatch input توسط React دیده نمی‌شود (value tracker) — باید native setter ویژگی value صدا زده شود: `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set.call(el, v)` سپس dispatch input.
- **دیالوگ Radix به body پورت می‌شود** — سنجه متن دیالوگ هرگز از `main` خوانده نمی‌شود؛ سلکتور `[role=alertdialog]/[role=dialog]` در سطح document.

## درس‌های تست/محیط R6 (P2-T13/T14 — ۱۴۰۵/۰۶/۱۲)

- **۲۵ — مرورگر تست بین اجراها نشست دارد:** `login()` کمکی روی نشست زنده short-circuit می‌کند (nav را می‌بیند → true بدون تعویض کاربر). تعویض کاربر = اول خروج سروری `fetch('/api/auth/logout', {method:'POST'})` + reload بعد از login — الگوی forceLogin در e2e-p2-t13-t14. علامت: «همه سبز به‌جز گارد دسترسی» = کاربر اجرای قبلی هنوز لاگین است (me را مستقیم با fetch('/api/auth/me') راستی‌آزمایی کنید).
- **۲۶ — path فقط-کوئری = درخواست ریشه سایت:** در helperهای api، path باید با مسیر کامل شروع شود؛ `fetch(BASE + '?from=…')` به '/' می‌رود → HTML با status 200 بی‌صدا (JSON.parse شکست می‌خورد و data.raw می‌ماند). علامت: «status 200 ولی content-type text/html» = مسیر جا افتاده، نه باگ سرور.
- **۲۷ — نام فایل Content-Disposition فقط ASCII:** رقم فارسی/جلالی در filename → `TypeError: Cannot convert argument to a ByteString` (هدر Latin-1 است) → 500. تاریخ نام فایل را از ISO بگیرید (`from.slice(0,10)`) نه رشته جلالی.
- **LLM زنده در تست E2E:** درخواست letter-assist واقعاً مدل را صدا می‌زند (~۱٫۵–۱۵ث) — poll با سقف ۹۰ث + شاخه ۵۰۳ «سرویس در دسترس نیست» را [SKIP] کنید نه FAIL (سنجه‌های اعمال/گارد در تست API پوشش داده شده‌اند؛ سنجه کارت در دسترس بودن مدل را لازم دارد).

## درس‌های تست/محیط R7 (P2-T16 — ۱۴۰۵/۰۶/۱۳)

- **۲۸ — `#` در ابتدای کلمه در sh کامنت است:** `ab("upload #ocr-file-input ...")` بی‌صدا بدون آرگومان اجرا می‌شود و «Command failed» برمی‌گردد — هر سلکتوری که با # یا . شروع می‌شود در رشته فرمان ab کوتیشن تک بگیرد (`upload '#ocr-file-input'`).
- **۲۹ — فلگ `--full` اسکرین‌شات agent-browser تصویر خالی می‌دهد** (فقط پس‌زمینه سفید ~۵KB): اسکرین‌شات viewport با ارتفاع کافی بگیرید و صحت محتوا را با حداقل سایز فایل (>۶KB) گارد کنید.
- **۳۰ — کش فلگ ۱۵ث + HMR در dev:** خاموش/روشن کردن Feature Flag از PATCH حاکمیت لزوماً بلافاصله در بسته route دیده نمی‌شود — تست فلگ باید poll کند (probe ارزان: تصویر خالی → ۴۲۲ یعنی گیت روشن؛ 503 یعنی خاموش) نه یک POST فوری.
- **۳۱ — سنجه‌های LLM ناکاتشی دو‌سطحی‌اند:** قرارداد/پاکت/گارد = FAIL سخت؛ کیفیت خروجی مدل (طبقه‌بندی/زبان) = retry یک‌بار + ثبت SOFT (نه FAIL) — کیفیت مدل باگ محصول نیست؛ در ضمن پیش‌نویس localStorage (io.draft.v1) بین اجراهای تست E2E نشت می‌کند و سنجه «پیش‌پر شدن» را آلوده می‌کند — قبل از تست پاک شود.
- **LLM زنده در OCR:** کل درخواست (tesseract ~۱–۴ث + مدل ~۲–۱۵ث) — poll سقف ۹۰ث؛ شاخه ۵۰۳ → [SKIP].

## درس‌های تست/محیط R8 (P2-T5 — ۱۴۰۵/۰۶/۱۴)

- **۳۲ — سرور Next روی Node اجرا می‌شود، نه Bun:** زنجیره فرایند `bun run dev → bash|tee → node next dev → next-server` است — ماژول‌های بومی Bun (مثل bun:sqlite) در src در زمان اجرا Crash می‌کنند (فقط اسکریپت‌های مستقل `bun scripts/…` می‌توانند). SQL خام در src فقط از `$queryRawUnsafe/$executeRawUnsafe` خود Prisma — پارامترهای Postgres شماره‌ای‌اند (`$1,$2,…`) نه `?` (پورت لایه سوم).
- **۳۳ — 'server-only' در bun خام throw می‌کند:** بسته فقط شرط `react-server` را به empty.js می‌رساند؛ مسیر default (اجرای مستقل اسکریپت) index.js پرتاب‌گر است. ماژولی که هم سرور هم seed مستقل مصرف می‌کند = بدون server-only + بدون import بسته Prisma (پاس CH-02) → کلاینت ساختاری حداقلی (الگوی FtsDbClient در fts-sql.ts؛ bivariance متدها اجازه تخصیص PrismaClient می‌دهد).
- **۳۵ — VACUUM روی db زنده = مرگ سرور dev (دوره SQLite — تاریخی):** با Neon این ریسک منتفی است؛ فایل `db/custom.db` آرشیو است و ابزارهای vacuum/db-snapshot گیت provider دارند.
- **۳۴ — رشته import در کامنت = false positive دروازه check:** parser چک CH-02 با regex می‌گردد و عبارت literal «import '@prisma/client'» داخل کامنت فارسی را import واقعی می‌شمارد و دروازه را قرمز می‌کند — در کامنت‌ها نام مسیر/بسته را با حروف جدا بنویسید («ایمپورت مستقیم بسته Prisma») نه رشته قابل-regex.

## درس‌های لایه سوم ماندگاری Neon (۱۴۰۵/۰۶/۱۰)

- **۳۶ — تزریق DATABASE_URL قدیمی در محیط shell سندباکس:** محیط گاهی `DATABASE_URL=file:…` (مقدار .env زمان بوت) را به‌صورت متغیر process به هر shell تزریق می‌کند که بر `.env` فعلی اولویت دارد — علامت: «the URL must start with the protocol postgresql://». رأی‌العمل: هر اجرای اسکریپت/سرور با `( unset DATABASE_URL; … )`؛ گارد fail-fast در `src/core/shared/db.ts` خطای فارسی واضح می‌دهد. unset در «همان» فرمان لازم است (بین فرمان‌های جدا پایدار نیست).
- **۳۷ — pgbouncer=true روی Neon = churn اتصال:** با `&pgbouncer=true` هر پرس‌وجوی آزاد اتصال تازه می‌سازد (~۱۰۵۰ms ثابت = TLS+SCRAM)؛ بدون آن PgBouncer مدرن Neon از prepared statementهای پروتکلی پشتیبانی می‌کند → بازاستفاده اتصال ~۲۱۳ms ≈ یک RTT (اثبات: scripts/probe-neon-matrix.ts — ۱۵ تکرار هم‌شکل پایدار). URL نهایی: sslmode=require&channel_binding=require — بدون pgbouncer.
- **۳۸ — Postgres نام مستعار بدون کوتیشن را lowercase می‌کند:** `SELECT … AS ftsC` → ستون `ftsc` برمی‌گردد و خوانش `row.ftsC` = undefined (این بسته: ensure فکر می‌کرد هر دو شمارش صفرند و rebuild هرگز اجرا نشد). نام‌های مستعار camelCase در SQL خام همیشه کوتیشن‌دار: `AS "ftsC"`.
- **۳۹ — دو تفاوت معنایی FTS5↔tsvector که پورت را می‌شکند:** (۱) توکنایز PG «1405/2655» را تک‌واژه می‌بیند (FTS5 دو واژه) → در numText جداکننده «/» به فاصله تبدیل شود؛ (۲) planner PG برای فیلتر پرانتخابی + LIMIT عمدا Seq Scan می‌زند (تصمیم درست) → باتری ایندکس باید «قابلیت» را با `SET LOCAL enable_seqscan=off` **در همان transaction** بسنجد (زیر PgBouncer transaction-mode اتصال بین statementها عوض می‌شود). بهینه WAN: ادغام دو COUNT در یک SELECT ترکیبی و صفحه+شمارش با `COUNT(*) OVER()`.
- **۴۰ — بودجه‌های کارایی SQLite محلی روی WAN معنا ندارند:** هر رفت‌وبرگشت ~۲۲۰ms — جستجو ~۲.۵–۳s و داشبورد ~۵s (هزینه صریح لایه سوم). باتری‌ها بودجه WAN دارند (t5: ۵s · list-contract داشبورد: ۶s)؛ e2e با `E2E_WAIT_SCALE=3` اجرا شود. پیگیری بهینه‌سازی: ادغام COUNTهای داشبورد با FILTER در یک SQL + کاهش رفت‌وبرگشت auth/list.
- **۴۱ — پارامترهای `$n` و کوتیشن شناسه‌ها:** همه SQL خام پورت‌شده باید شناسه‌های camelCase را کوتیشن‌دار بدهد (`"Letter"`, `"letterId"` — بدون آن lowercase و ناموجود) و پارامترها `$1..$n` باشند؛ درج دسته‌ای rebuild به ۵۰۰×۶=۳۰۰۰ پارامتر (سقف PG ۶۵۵۳۵) رسید. HAVING فقط روی خود عبارتaggregate می‌پذیرد نه alias (`HAVING COUNT(*) > 1`).

## باگ‌های شناخته‌شده محیط (پرهیز کنید)

1. Radix Select با `value=""` کرش می‌کند → همیشه مقدار نگهبان مثل `'none'`
2. الگوریتم تبدیل جلالی با mod/div ساده غلط است → فقط پیاده‌سازی reference شده
3. شماره‌گذاری سند باید per-company باشد (`DocCounter`) نه سراسری — unique constraint
4. `setInterval` + `setState` بدون alive-flag → خطای hydration/leak
5. socket.io با `path:'/'` کل HTTP سرور خودش را تصاحب می‌کند → API داخلی روی پورت جداگانه
6. fetch مرورگر به `127.0.0.1:3000` برای تست اتصال سوکت کار نمی‌کند → تست از پورت ۸۱ (گیت‌وی)
7. `truncate` روی `dev.log` فایل اسپارس پر از بایت تهی می‌سازد (خط‌لوله `tee` محیط offset خودش را نگه می‌دارد) → فقط لاگ سرویس بلادرنگ truncate شود (RB-02). محل لاگ به راه‌انداز بستگی دارد: راه‌انداز محیط `.zscripts/mini-service-realtime.log` می‌نویسد؛ احیای دستی طبق RB-01 داخل `mini-services/realtime/` (فایل هنگام اجرا ساخته می‌شود)
8. `pkill -f "mini-services/realtime"` پروسه سرویس را نمی‌کشد (cmdline فقط `bun run dev` است) → kill با pid از `ss -tlnp | grep 3003` (RB-01)
9. سرور Next توسط OOM killer ساکت کشته می‌شود (بدون رد کرش در dev.log؛ باکس ۴GB) و محیط نگهبان ندارد → فرمان احیا در RB-01؛ مرورگر تست را بعد از اتمام ببندید
10. **کوکی نشست در پنل پیش‌نمایش (iframe سایت‌متقاطع) مسدود می‌شود** — علامت: `POST /api/auth/login` برمی‌گردد ۲۰۰ ولی `GET /api/auth/me` بلافاصله ۴۰۱ می‌شود و کاربر به صفحه ورود برمی‌گردد (کوکی SameSite=Lax در بافت third-party ذخیره/ارسال نمی‌شود). رفع‌شده با پشتیبان هدر `x-session-token` (الگوی «احراز هویت دومسیره» بالا)؛ کوکی در بستر HTTPS/دامنه پیش‌نمایش به‌صورت خودکار `SameSite=None; Secure` صادر می‌شود (`sessionCookieAttrs`). برای بازتولید/تست: `scripts/iframe-host/` (میزبان localhost:3005 با iframe به 127.0.0.1:81) — اجرا: `( cd scripts/iframe-host && setsid bun run serve.ts >> serve.log 2>&1 < /dev/null & )`
11. **git stash دیتابیس بسته‌شده را فقط‌خواندنی می‌کند** — `db/custom.db` در git ردیابی می‌شود؛ stash/pop فایل را با inode جدید می‌نویسد و پروسه سرورِ در حال اجرا به inode قدیمی اشاره می‌کند → هر نوشتن با «attempt to write a readonly database» (500) شکست می‌خورد. رفع: احیا کامل زنجیره طبق RB-01. **هرگز با سرور روشن stash/checkout روی db نکنید**؛ برای baseline تایپ، مقایسه خروجی tsc را در فایل نگه دارید

12. **کلیک JS خام (element.click) روی اجزای Radix فعال‌سازی نمی‌کند** — TabsTrigger/DropdownMenuTrigger/SelectTrigger به رویدادهای pointer واقعی نیاز دارند؛ در اتوماسیون مرورگر از فرمان بومی استفاده کنید (`agent-browser find role tab|combobox|button click --name "..."`)
13. **سشن پیش‌فرض agent-browser بین همه agentهای ماشین مشترک است** — agent دیگر می‌تواند صفحه را mid-test ناوبری کند (علامت: اسکرین‌شات صفحه‌ای خارج از اپ)؛ برای تست‌های حساس سشن نام‌دار: `agent-browser --session <name> ...`
14. **ری‌استارت سرور dev پس از prisma db push الزامی است** — کلاینت Prisma در حافظه پروسه قدیمی می‌ماند و ستون جدید با «Unknown argument» 500 می‌دهد؛ احیای سرور طبق RB-01 با الگوی یتیم‌سازی double-fork: `( unset DATABASE_URL; cd /home/z/my-project && setsid bash -c 'exec node node_modules/.bin/next dev -p 3000 > dev.log 2>&1' < /dev/null & ) — با unset (لایه سوم/درس ۳۶)` تا از کشته‌شدن بین فرمان‌ها در امان بماند
15. **دوگانگی سرویس بلادرنگ پس از kill** — SIGTERM خاموشی آرام دارد (io.close منتظر سوکت‌ها می‌ماند)؛ پروسه زامبی بدون پورت باقی می‌ماند و نسخه تازه پورت‌ها را می‌گیرد — عادی است، همیشه `ss -tlnp | grep 3003` صاحب واقعی را نشان می‌دهد (پورت‌ها = مرجع حقیقت، نه شمار پروسه)
16. **نمایش ترمینال متن فارسی+براکت را جابه‌جا می‌کند** — `[me?.activeCompanyId, rtVersion]` با کامنت فارسی مجاور ممکن است در cat/sed به شکل `}, e?.activeCompanyId, rtVersion])` دیده شود؛ قبل از نتیجه‌گیری درباره syntax، بایت‌ها را ببینید (python repr یا hexdump)
17. **منوی Radix بازمانده، `find role` اتوماسیون را کور می‌کند** — آیتم‌های منوی DataGrid (مثل «اندازه صفحه») با `onSelect={(e) => e.preventDefault()}` عمداً باز می‌مانند؛ Radix پس‌زمینه را `aria-hidden` می‌کند و `agent-browser find role button` فقط دکمه‌های بیرونی (مثل نشانگر Next DevTools) را می‌بیند → `logout()` بی‌صدا می‌شکند و login بعدی با کاربر قبلی «قبلاً وارد شده» برمی‌گردد (علامت: تست داشبورد به‌جای KPI، دکمه‌های toolbar نمای دیگری می‌بیند). رفع در helpers: logout اول با Escape روی document هر `[data-state="open"]` را می‌بندد + راستی‌آزمایی فرم ورود + یک تلاش مجدد؛ تست‌ها هم بهداشت خودشان را دارند (بستن منو در پایان — نمونه: t35)
18. **`sessionStorage.io.workspace.v1` بین کاربران/تست‌ها نشت می‌کند** — ورود جدید همان workspace نشست قبلی را بازیابی می‌کند (SPA بدون reload)؛ تستی که فرضش داشبوردِ پیش‌فرض بعد از ورود است باید قبل از login این کلید را removeItem کند (نمونه: test-dashboard-v2 — درس ترتیب تست‌ها پس از t35)
19. **Radix TabsTrigger فقط با mousedown فعال می‌شود نه click** (درس U5): `element.click()` روی تب داخلی هیچ اثری ندارد — کد منبع Radix `onMouseDown` است که `onValueChange` را صدا می‌زند. در eval تست: `t.dispatchEvent(new MouseEvent('mousedown', {bubbles: true, cancelable: true, button: 0}))`. (DropdownMenu برعکس: pointerdown لازم دارد — بند ۱۲؛ هر کامپوننت رویداد خودش را می‌خواهد — منبع حقیقت: `node_modules/@radix-ui/*/dist/index.mjs`)
20. **شمارش تب در تست‌ها باید به نوار «تب‌های کاری» محدود باشد** (درس U5): `[role=tab]` سراسری بعد از innerTabs صفحه رکورد (U5/U10) دو معنا دارد؛ سلکتور صحیح: `[role=tablist][aria-label="تب‌های کاری"] [role=tab]`. همچنین «کلیک تب با متن» را به `main` محدود کنید تا تب کاریِ هم‌نام (مثل عنوان «سند آزمون خط زمان») هدف اشتباه نشود
21. **مرگ ساکت سرور dev = شکست زنجیره‌ای تست‌ها** (OOM، بند ۹): علامت `curl api:000`؛ قبل از هر باتری تست، سلامت سرور را با `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3000/api` بگیرید و اگر مرد با الگوی double-fork (بند ۱۴) احیا کنید — شکست تست‌ها پس از آن تفسیر واقعی خودشان را می‌یابند (درس U5: t35/u3 به‌ظاهر قرمز، فقط سرور مرده بود)
22. **پورتال غیر-Radix باید `data-state="open"` بگیرد** (درس U7): گارد Esc سراسری `anyOverlayOpen()` صرفاً `[data-state="open"]` می‌سنجد (قرارداد Radix). پورتال سفارشی (مثل پورتال چاپ) بدون این صفت = شبح: Esc هم پورتال را می‌بندد و هم تب فعال را — و چون هر دو listener روی window ثبت‌اند stopPropagation راه‌حل نیست (هم‌هدف = ترتیب ثبت). علامت: پس از Esc، شمار تب یک کم می‌شود هم‌زمان با بسته شدن پورتال. هر پورتال شناور جدید: `data-state="open"` روی ریشه + listener Esc خودش.
23. **IN بزرگ (≥۱۰۰۰) + orderBy در موتور Prisma/SQLite = RustPanic** (درس T10/T11 — `record.rs: no entry found for key`): فیلتر `letterId: { in: [...1202] }` با `orderBy` سروری 500 می‌دهد؛ همان کوئری بدون orderBy با ۳۰۰۰ آیتم سبز است (دوبیسه شد: ۵۰۰=سبز، ۱۰۰۰=پانیک). قانون: برای مجموعه‌های بزرگ، برداشت بدون orderBy + مرتب‌سازی/«آخرین» را در JS محاسبه کنید؛ `take:1` تو در تو روی رابطه چندمقداری + فیلتر some هم مسیر پرخطر است (صفحه‌های ≤۳۰ ردیف مشکلی ندارند).
24. **runnerهای تازه زمان‌بند در فرایند زنده دیده نمی‌شوند مگر با رجیستری globalThis** (درس T11): حلقه تیک در closure نمونه اولیه ماژول scheduler می‌ماند و ثبت runner از نمونه‌های HMR تازه به آن نمی‌رسد — رجیستری را روی `globalThis.__ideaoneJobRunners` نگه دارید + ثبت استاتی در خود ماژول scheduler (نه فقط instrumentation) تا هر نمونه‌ای همان Map را ببیند؛ در بوت تازه فرایند همه‌چیز استاتیک ثبت می‌شود. (تکمیل بند ۱۴: نگهبان `PRISMA_GEN` در db.ts کمربند اول است اما Turbopack ماژول‌های node_modules را بازخوانی نمی‌کند — در عمل پس از db push، احیای سرور با double-fork راه مطمئن است.)

## مستندات مرتبط — نقشه کامل: `docs/README.md`

| نیاز | مسیر |
|---|---|
| نقشه مستندات + قواعد نگهداشت | `docs/README.md` |
| تاکسونومی سه‌لایه پلاگین‌ها | `docs/adr/ADR-008-module-taxonomy.md` |
| سرویس‌های هسته (۶ سرویس جدید) | `docs/adr/ADR-009-core-services.md` |
| مشخصات هر پلاگین (فرم فیلد‌به‌فیلد/نما/API/مجوز) | `docs/modules/<code>/SPEC.md` (قالب: `_TEMPLATE.md`) |
| مدل داده فیلد‌به‌فیلد (۳۵ مدل) | `docs/architecture/03-data-model.md` |
| قواعد پوشه‌بندی + سیاست «بدون فایل منسوخ» | `docs/architecture/02-folder-structure.md` |
| ماتریس امنیت/RBAC | `docs/architecture/04-security-rbac.md` |
| نقشه راه (۱۱ فاز، ۲۸۷ وظیفه با شناسه Pn-Tkk) | `docs/roadmap/00-master.md` |
| سناریوهای عملیاتی (SC-001 تا SC-008) | `docs/scenarios/` |
| تصمیم‌های معماری | `docs/adr/` (ADR-001 تا 009) |
| دروازه کیفیت ساختار/مستندات | `bun run check` ← `scripts/check.ts` (ADR-007) |
| عملیات سندباکس | `docs/runbook/` |
| فارسی و انطباق ایران | `docs/persian/` |
| ماتریس ممیزی و فازبندی | `download/ماتریس ممیزی و نگاشت سندباکس.xlsx` |
| گزارش‌های گیت (تولید خودکار) | `bunx tsx scripts/gate-report.ts` → `download/گزارش-گیت-۱-*.md` |
| سند اصلی محصول | `upload/ideaone idea.txt` |
| پژوهش‌های بنچمارک (Odoo/Axelor/تیم‌یار/راهکاران) | `research/` |

**قانون مستندات**: هر تغییر رفتاری (فیلد فرم/ستون نما/endpoint/مدل داده) در همان PR سند مربوطه را به‌روزرسانی می‌کند — بدون به‌روزرسانی سند، PR رد می‌شود. ارجاع وظیفه در کد: `// TODO(Pn-Tkk): …`.
