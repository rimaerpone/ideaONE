# طرح بازیابی — ایستگاه ۱۴۰۵/۰۶/۰۹ پس از قطع محیط

وضعیت: **فعال** | درج: ۱۴۰۵/۰۶/۰۹ (پس از قطع) | مالک: معماری
رخداد: محیط سندباکس در میانه جلسه به آخرین ایستگاه پایدار git بازگردانده شد (کامیت `decd5fe` — پایان P2.5 با ۵/۸). کارهای پس از آن ایستگاه که در git ذخیره نشده بودند از دیسک حذف شدند. این سند = دانش بازیابی از حافظه گفتگو + ترتیب بازسازی.

## ۱. وضعیت واقعی فعلی (ممیز)

- **موجود**: P0 کامل · G0 · P1 کامل ۳۷/۳۷ · P1.5 کامل ۱۴/۱۴ · P2.5 پنج‌هشتم (U1 عرض/سکشن · U2 انتخاب گروهی · U3 شخصی‌سازی io.ui.v1 · U4 پیش‌نمایش کنار فهرست · U8 داشبورد نسل ۲) · P2 دو‌بیست‌وششمی (T1+T2 تقریبی)
- **اجزای موجود در کد** (بررسی شده): bulk-action-bar · preview-panel · form-section · ui-prefs · ui-bits · data-grid (سرور pagination/sort) · jalali-date-picker · search-select · confirm-dialog · draft-banner · warehouses-admin/previewها · office-automation: service/queries + ۴ کامپوننت (بدون FTS/OCR/الگو/ایمیل/امضا/آمار/بایگانی/شماره‌گذاری پیشرفته) · **بدون** record-timeline · csv.ts · recent-views · use-csv-export · FTS · نوار تب پین‌دار (pinned در workspace نیست؛ closeOthers هست)
- **ناپدید (باید بازسازی شود)**: همه‌چیز پس از U8 — فهرست در §۲

## ۲. صف بازسازی (به ترتیب ارزش/وابستگی — دستور پخت از ورک‌لاگ مکالمه)

| # | بسته | محتوای کلیدی (دستور پخت فشرده) | حجم/وضعیت |
|---|---|---|---|
| R0 | **P0.5 بسته تثبیت اعتماد** | تراکنش `applyDocToStock` + گارد اتمیک `actOnLetter` + ۱۰–۱۵ تست واحد (یافته C1/C2 ممیزی عمیق) + هم‌راستاسازی رجیستری با ۳۳ ماژول مصوب + CI/CSRF/scrypt async — [فایل فاز](./roadmap/P0.5-stabilization.md) | **در اجرا — T1 انجام‌شده ۱۴۰۵/۰۶/۱۶؛ T2/T3 باقی** |
| R1 | **P2.5-U5 خط زمان رکورد** | کامپوننت مشترک `RecordTimeline` (components/common/record-timeline) + API `GET /api/audit/timeline?entity=&id=` (سرویس entityTimeline در core/audit؛ لیست‌سفید نهادها + گارد scopeCompanyIds) + هوک useRecordTimelineQuery + QK_PREFIX.timeline + innerTabs روی RecordPageShell (سند/درخواست/محصول؛ نامه عمداً نه) + ACTION_FA برچسب‌ها | **✅ اجرا شد ۱۴۰۵/۰۶/۱۰** (۳۰/۳۰ سنجه؛ مصرف‌کننده سند/درخواست/کاربر — محصول در ادامه R2) |
| R2 | **P2.5-U6 خروجی per-view** | `core/shared/csv` (CSV_ROW_CAP=5000 · buildCsvDocument · csvFilename · csvFileResponse + X-Csv-Rows/X-Csv-Capped) + هوک use-csv-export + دکمه در نماها (حسابرسی/نامه/سند/موجودی/درخواست) با همان where-builder فهرست + BOM تست با بایت خام | **✅ اجرا شد ۱۴۰۵/۰۶/۱۰** (۲۵/۲۵ — csv.ts + اسناد/موجودی؛ نامه‌ها/درخواست/حسابرسیِ دیگر در U7 و P2-T20) |
| R3 | **P2.5-U7 چاپ سربرگ‌دار** | LetterPrintSheet از طریق portal به body + `.print-portal` + `@media print` + کلید letters.letterheadText per-company + چاپ نامه + (پس از T24) امضا روی خانه‌ها | **✅ اجرا شد ۱۴۰۵/۰۶/۱۱** (۵۱/۵۱ — LetterPrintDialog با .letter-print-root/@page A4 + دو کلید letterhead.subtitle/footer در CompanySetting + UI تنظیم در تب شرکت + سجل حاکمیتی PRINT + CSV نامه‌ها P2-T20؛ T24 امضای تصویری باقی) |
| R4 | **P2-T4..T6 هسته نامه v2** | ثبت با شماره‌گذاری پیش‌فرض + عطف اولیه + جعبه‌های وارده/صادره/داخلی + وضعیت‌ها + پرچم‌های محرمانگی/فوریت | L |
| R5 | **P2-T10/T11 مهلت+یادآور** | deadlineAt در LetterReferral + استک HELEVAN + فیلتر/نشان + runner ماژولی «deadline-reminder» ساعتی (registerJobRunner از ریشه instrumentation + ردیف ScheduledJob idempotent) | **✅ اجرا شد ۱۴۰۵/۰۶/۱۲** (۷۰ سنجه — runner در core/scheduler/jobs + رجیستری سراسری + Notification.dedupKey یکتا برای عدم اسپم + اجرای دستی ادمین از حاکمیت؛ «استک HELEVAN» به زیرساخت موجود notify/ScheduledJob ترجمه شد) |
| R6 | **P2-T13/T14 گزارش هفتگی + AI editable** | srunning weekly-report (هفته ایرانی شنبه‌جمعه؛ ۵ شاخص؛ MD/چاپ/کپی؛ گارد مدیر؛ منوی رجیستری idempotent add-weekly-report-menu.ts) + کارت AI ویرایش‌پذیر + AiSuggestion applied/edited | **✅ اجرا شد ۱۴۰۵/۰۶/۱۲** (۷۸ سنجه — buildCartableWeeklyReport با منبع LetterReferral (به‌جای AuditLog ناقص) + preset/staleDays + ?format=md + پورتال چاپ؛ کارت AI ویرایش‌پذیر با ai-categories.ts مشترک و سجل مقادیر نهایی؛ جدول AiSuggestion مستقل به T15 موکول شد — تاریخچه از سجل AI_SUGGEST/AI_APPLY قابل بازسازی؛ «منوی رجیستری» لازم نشد — کارت داخل تنظیمات←یکپارچه‌سازی) |
| R7 | **P2-T16 OCR فارسی** | دو مرحله‌ای: tesseract بومی (`.tessdata/fas.traineddata`؛ `-l fas --psm 6`) + ساختاردهی LLM از مسیر runAiJson؛ ماژول ocr.ts بی‌حالت؛ فرم آپلود → ویرایش متن → ثبت نامه؛ فلگ + requireWriteRole | **✅ اجرا شد ۱۴۰۵/۰۶/۱۳** (~۵۰ API + ۲۲ E2E — ocr.ts در modules/office-automation (۱۸ سرویس هسته ADR-009 کامل است؛ ارتقای آتی به core با ADR جدید)؛ fas.traineddata ۳٫۲MB کامیت شد؛ + حذف مکانیکی سربرگ از body (پادزهر ناکاتشی LLM) + HITL merge-نه-replace + سیاست «سری» + فلگ‌های letters.ocr/ai.letter-ocr + سجل OCR) |
| R8 | **P2-T5 FTS نرمال‌شده** | جدول letter_fts خارج Prisma (fts.ts خودترمیم + fts-sql.ts مشترک + rebuild پس از seed) + normalizeFaText + واژه ≥۲ نویسه + پیشوند* + شماره دقیق + هایلایت HighlightFa regex یونیکدی + عقب‌گرد contains | **✅ اجرا شد ۱۴۰۵/۰۶/۱۴** (۵۱ API/DB + ۱۴ E2E — FTS5 از مسیر $queryRawUnsafe خود Prisma چون سرور روی Node است؛ کلاینت ساختاری FtsDbClient برای seedها؛ شماره نمایشی «سال/شماره» هم قابل جستجو؛ CSV آینه FTS؛ کارایی ۵۳–۸۶ms روی ۱۰٬۲۴۸ نامه) |
| R9 | **P2-T8/T9 شماره‌گذاری/عطف** | LetterNumbering per-type (types/platform مشترک) + numbering.ts + displayNumber سرورساخته + انتخاب‌گر عطف Popover با جستجوی سروری + زنجیره ۵ سطحی در تب رکورد + گارد دامنه | M |
| R10 | **P2-T12/T15/T17/T21** | معطل‌ها (stuckWhere در core/tenancy — داشبورد+اداری) + آستانه per-company · تاریخچه AiSuggestion + hint موضوع (بدون بدنه!) · بایگانی موضوعی درخت ۳ سطحی + برچسب + فیلتر زیردرخت | L |
| R11 | **P2-T22 الگو** | LetterTemplate + renderLetterTemplate در types/platform (نام‌مستعار فارسی) + انتخاب‌گر در فرم + ۵ seed + تب مدیریت | M |
| R12 | **P2-T23 ایمیل دبیرخانه** | imapflow poll ۱۵ دقیقه + mailparser → DRAFT وارده + پیوست .eml + حذف تکرار Message-ID + گذرواژه mask/ادغami + MAIL_INGEST | L |
| R13 | **P2-T24 امضای تصویری** | UserSignature یکتا per-کاربر + آپلود در حساب من + رندر فقط در print-portal با apiBlob/x-session-token | M |
| R14 | **P2-T25 محرمانگی** | confidentialLettersVisible در core/tenancy (دارنده+سازنده نقش‌گریز) + lettersWhere آرایه AND + ۴۰۴ اوراکل بسته در ۱۰ مسیر + فیلتر زنجیره عطف | M |
| R15 | **P2-T26 آمار + T19 فیلتر ترکیبی** | stats.ts ماه جلالی سطل‌بندی سرور + نمودار BARD RTL + سه Select نوع×وضعیت×فوریت | M |
| R16 | **P2.7 ناوبری نسل ۲** | navSection در PlatformModule (۶ گروه؛ seed؛ migrate-navsection.ts) + کارتابل واحد /api/cartable/v2 + اقدام سریع + Recent در پالت (recent-views.ts io.ui.v1) + جستجوی سند در پالت + راست‌کلیک/پین/سرریز تب (store/workspace pinned + قواعد RTL) + ADR-011 + ADR-010 تدارکات | L |
| R17 | **گیت G2** | test-gate-metrics/gate-report-2 → گزارش جلسه؛ معیارهای جدول master §۱ | S |

> اصل ترتیب: R1→R2→R3 (زیرساخت مشترک باقی صف)؛ سپس R4..R15 هسته P2؛ R16 ناوبری؛ R17 گیت. هر بسته = تست اختصاصی + مستندات + کامیت Conventional **بلافاصله** (درس این رخداد: بدون کامیت، بازگشت محیط = داده مفقود).
>
> **به‌روزرسانی ترتیب ۱۴۰۵/۰۶/۱۱:** پس از R1+R2، طبق بازنگری نقشه راه P2.5 §۲ (پژوهش ۰۲ — خواسته کارفرما)، **U9 نیم‌صفحه FCL و U10 استاندارد تب داخلی** اجرا شدند (خارج از صف R — وظایف جدید)؛ سپس **R3 چاپ سربرگ (P2.5-U7) اجرا شد و P2.5 کامل بست (۱۰/۱۰)**.
>
> **به‌روزرسانی ۱۴۰۵/۰۶/۱۲:** **R5 (P2-T10/T11 مهلت ارجاع + یادآور خودکار) اجرا شد** — اولین بسته هسته P2 پس از بستن P2.5 (۷۰ سنجه سبز). گام بعدی طبق اصل ترتیب: **R6 (P2-T13/T14 گزارش هفتگی + AI ویرایش‌پذیر)** یا طبق بازنگری کارفرما.
>
> **به‌روزرسانی ۱۴۰۵/۰۶/۱۳:** **R7 (P2-T16 OCR فارسی) اجرا شد** — ۵ نمونه فارسی ۱۰۰٪ توکن‌کلیدی؛ P2 هسته اکنون ۹/۲۶. گام بعدی: **R8 (P2-T5 FTS نرمال‌شده)** طبق صف؛ در موازی انتظار کارفرما: connection string Neon (لایه سوم ماندگاری).
>
> **به‌روزرسانی ۱۴۰۵/۰۶/۱۰ (لایه سوم ماندگاری):** connection string Neon از کارفرما رسید و **مهاجرت کامل SQLite→Neon Postgres اجرا شد** — ۵۴٬۵۶۱ ردیف/۳۴ جدول با ترتیب توپولوژیک FK + تبدیل نوع + راستی‌آزمایی شمارش؛ پورت fts-sql به tsvector/GIN (هم‌ارزی معنایی کامل: استعلام=۴۷۶، قیمت=۲۴۶، مکاتبات=۳٬۵۳۳)؛ رگرسیون ۱۳ باتری سبز (check ۲۵/۲۵ · tsc · lint · t5 ۵۱/۵۱ · e2e-t5 ۱۴/۱۴ · golden ۸/۸ با E2E_WAIT_SCALE=۳ · t10-t11 · t4-t6 · rbac · list-contract · login-security · record-pages · indexes)؛ PRISMA_GEN=gen-2026-09-01-neon. هزینه WAN صریح: جستجو ~۲.۵–۳s · داشبورد ~۵s (RTT ~۲۲۰ms). گام بعدی: **R9 (P2-T8/T9)** طبق صف — لایه‌های ماندگاری هر سه فعال (دیسک + GitHub + Neon).
>
> **به‌روزرسانی ۱۴۰۵/۰۶/۱۴:** **R8 (P2-T5 FTS نرمال‌شده) اجرا شد** — ۵۱ سنجه API/DB + ۱۴ E2E + رگرسیون کامل ۲۷ باتری سبز؛ P2 هسته اکنون ۱۰/۲۶. گام بعدی: **R9 (P2-T8/T9 شماره‌گذاری per-type + عطف دوسویه)** طبق صف؛ در موازی انتظار کارفرما: connection string Neon (لایه سوم ماندگاری).
>
> **به‌روزرسانی ۱۴۰۵/۰۶/۱۲ (بسته دوم):** **R6 (P2-T13/T14) اجرا شد** — ۷۸ سنجه (۴۸ API + ۳۰ E2E با LLM زنده)؛ P2 هسته اکنون ۸/۲۶. گام بعدی: **R7 (P2-T16 OCR فارسی)** یا R8 طبق صف؛ در موازی انتظار کارفرما: connection string Neon (لایه سوم ماندگاری).
>
> **به‌روزرسانی ۱۴۰۵/۰۶/۱۵ (بستهٔ پیشنهادی):** گزارش نقشه راه واحد ۱۸گامی (بازخوانی چهار گزارش قبلی — worklog: `unified-roadmap-2026-09-01`) نتیجه گرفت پیش از ادامهٔ صف R، بستهٔ **R0/P0.5 تثبیت** (تراکنش + تست واحد + هم‌راستاسازی رجیستری ۳۳ + CI) ارزش بیشتری از R9 دارد چون با هر بستهٔ بعدی داده بیشتری با ساختار فعلی قفل می‌شود؛ اجرای R0 **پیش یا بلافاصله پس از R9** نیازمند حکم کارفرماست — ماتریس کامل وضعیت ماژول‌ها: `docs/product/04-module-coverage.md`.
>
> **به‌روزرسانی ۱۴۰۵/۰۶/۱۶ (R0 در اجرا — T1 بسته شد):** کارفرما با «ادامه بده» مسیر **تثبیت-اول** را برگزید. `applyDocToStock` داخل `$transaction` تعاملی با claim اتمیک DRAFT→POSTED (رفع C1 + رقابت دوبار POST) · گارد هم‌زمانی خوش‌بینانهٔ `actOnLetter` (یک برنده، دیگری 409 بدون ارجاع یتیم) · ۱۶ تست واحد (`bun test scripts/unit` — mock درون‌حافظه‌ای با rollback واقعی) + ۲۳ سنجهٔ e2e زنده روی Neon (`scripts/test-p05-t1-e2e.ts`) + باتری t4-t6 سبز · `ADR-010` پروتکل اجرای نشست (۵ مرحله + تاب‌آوری سندباکس) الزامی شد. گام بعدی: **P0.5-T2 هم‌راستاسازی رجیستری ۳۳ (نیازمند صدور CMD)** → T3.
>
> درس ثبت‌شده: **هر بسته را پس از سبز شدن تست‌ها همان لحظه git commit کنید** — ایستگاه‌های خودکار محیط فقط تا آخرین کامیت پایدارند.
>
> **گسترش R16 (۱۴۰۵/۰۶/۱۰ — پژوهش ۰۲ تب‌محوری نسل ۳):** دامنه R16 علاوه بر موارد قبلی شامل این پیشنهادهای کارفرما هم می‌شود: سرریز هوشمند «+N» به‌جای اسکرول بی‌انتها (#۳) · میان‌برهای `Ctrl+Tab/Ctrl+W/Alt+عدد` (#۲۱) · ناوبری فلشی نوار تب (#۲۲) · بازگشایی تب بسته‌شده با `Ctrl+Shift+T` (#۵) · درگ جابه‌جایی تب (#۶) · دکمه «+» نماها در انتهای نوار (#۷). جزئیات و ارجاع‌ها: [`research/02-tab-shell-v3-and-theme.md`](./research/02-tab-shell-v3-and-theme.md) §۴-A/D.

## ۳. کار جاری مستقل از صف (خواسته کارفرما — «دستورالعمل کدگذاری محصولات»)

موتور کدگذاری ساختارمحور «کد به‌عنوان جمله» — مستقل از ایستگاه، پایه مستردیتا:
- ۳ مدل (CodeScheme/CodeSegment/CodeEnumValue) **به شِما push شده و جداول در DB موجودند** ✓
- طرحواره کاشی کامل از سند شرکت (۱۶ جزء، ۲۰ کاراکتر، مادر = ۱۲ کاراکتر/۹ جزء) + ۳ طرحواره عمومی (تجهیزات/قطعات یدکی/مواد اولیه) = اثبات فراگیر بودن
- سرویس core/coding (compose/parse/describe/counter از nextDocNumber با scope CODE:*) + APIها + کدساز زنده در فرم کالا + رمزگشا
- جزئیات در SPEC محصولات پس از اجرا

## ۴. قواعد اجرایی بازسازی

1. هر بسته: بازخوانی فایل‌های همان حوزه در **وضعیت فعلی** (نه حافظه — نسخه‌ها فرق دارند)، اعمال دستور پخت، تست اختصاصی، `bun run check`، کامیت.
2. مستنداتِ عقب‌تر از ایستگاه (SPEC v1.5/v1.6، roadmapهای «کامل») در git به نسخه ایستگاه برگشته‌اند — با هر بسته دوباره به‌روز می‌شوند؛ هیچ ادعای «کامل» بدون کدِ موجود در دیسک.
3. تست‌های ازدست‌رفته (test-p2-remaining · test-p2-t22-t26 · …) با همان بسته‌ها بازسازی می‌شوند.
