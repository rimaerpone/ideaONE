# فاز P2 — اتوماسیون اداری نسل ۲ (Letters v2)

وضعیت: **در جریان** (۱۰ از ۲۶ وظیفه نقشه راه: T4 متن پاسخ + T6 قالب شماره سالانه + T7 چاپ سربرگ + T10 مهلت ارجاع + T11 یادآور خودکار + T13 گزارش هفتگی کارتابل + T14 ویرایش پیشنهاد AI + T16 OCR فارسی + T20 خروجی اکسل + **T5 جستجوی تمام‌متن FTS** + **گذر کیفیت «بررسی عمیق فرم‌ها» ۱۴۰۵/۰۶ — خارج از شمارش وظایف**: ۶۲ بهبود اجراشده روی هر ۱۱ فرم پروژه؛ باقیمانده در لیست انتظار `00-master.md §۶`) | گیت: مشارکت در **G2** (با P1) | پیش‌نیاز: G0؛ هم‌زمان با P4 ممکن
هدف: دبیرخانه واقعی بتواند «کل چرخه نامه» را بدون ابزار جانبی انجام دهد: پیوست، جستجوی سریع، چاپ، یادآور مهلت، پاسخ متنی، عطف.

> **گذر کیفیت فرم‌ها (خلاصه — جزئیات در worklog):** اجزای مشترک جدید `PasswordInput`/`PasswordStrength`/`CharCount`/`KbdHint`/`RowWarning` در form-bits + `faOptionalNumber` در schemas؛ گیرنده ارجاع اولیه نامه به SearchSelect جستجویی؛ شمارنده نویسه موضوع/متن/پاسخ/یادداشت؛ هشدار مهلت گذشته؛ تأیید حذف پیش‌نویس هنگام انصراف فرم dirty؛ **نمایش موجودی زنده کالا در فرم سند (کل + واریانت) با هشدار بیش‌برداشت** و در فرم درخواست با هشدار کمبود؛ هشدار قلم/کالای تکراری؛ datalist طرف حساب از شرکا؛ اعتبارسنجی عددی کارتن محصول (رفع تبدیل بی‌صفر ورودی نامعتبر)؛ بررسی زنده کد تکراری محصول؛ پیشنهاد خودکار کد انبار + هشدار نام تکراری؛ پیش‌نویس فرم ایجاد کاربر + سنجه قدرت گذرواژه + dirty tracking ویرایش‌ها + تأیید بستن تب dirty؛ دکمه «خروج از سایر دستگاه‌ها»؛ شاخص مطابقت تکرار گذرواژه؛ خطای inline سقف اعلان + Enter=ذخیره؛ چیپ‌های فیلتر حسابرسی + هشدار بازه معکوس + debounce جستجو.

## E-P2.1 — پیوست و پرونده نامه

| ID | وظیفه | معیار پذیرش | وابستگی | اندازه | وضعیت |
|---|---|---|---|---|---|
| P2-T1 | مدل `LetterAttachment` (id, letterId, fileName, mime, size, storagePath, createdById) + مهاجرت docs | schema + data-model به‌روزر؛ رکورد با audit | G0 | M |
| P2-T2 | آپلود پیوست (PDF/تصویر ≤ ۵MB، حداکثر ۱۰ فایل/نامه) با ذخیره در `db/attachments/` خارج public | انواع مجاز اعمال؛ نام فایل فارسی ایمن؛ خطای حجم فارسی | P2-T1 | M |
| P2-T3 | پیش‌نمایش PDF/تصویر در دیالوگ نامه + دانلود با نام اصلی | پیش‌نمایش inline؛ دانلود از download API با گارد دامنه | P2-T2 | M |
| P2-T4 | متن پاسخ: فیلد `answerText` روی LetterReferral (اقدام ANSWER با متن الزامی) | پاسخ در timeline با متن نمایش؛ اعتبارسنجی غیرخالی | G0 | S | **انجام** (ستون answerText در LetterReferral (db push) + اعتبارسنجی سرور: غیرخالی/trim + سقف ۵٬۰۰۰ نویسه با خطای فارسی؛ ذخیره فقط روی اقدام ANSWER (ARCHIVE با answerText → null)؛ UI: دکمه «ثبت پاسخ» پنل درون‌خطی با textarea الزامی + دکمه غیرفعال تا تایپ + خطای آینه سرور — دیگر اقدام بدون متن ممکن نیست؛ timeline: بلوک برجسته متن پاسخ مجزا از یادداشت؛ API: ۱۵ سنجه scripts/test-t4-t6-letters-v2.ts + E2E ۱۶ سنجه scripts/e2e-t4-answer.ts همه سبز |
| P2-T5 | جستجوی متن کامل FTS5 (subject/body/sender/receiver + شماره) با هایلایت نتیجه | جستجوی «مهر» در < 200ms روی seed:big؛ هایلایت فارسی | P1-T12 | L | **انجام** (۱۴۰۵/۰۶/۱۴ — جدول مجازی `letter_fts` خارج Prisma (FTS5 در موتور SQLite خود Prisma — probe اثبات‌شده؛ سرور روی Node است و bun:sqlite ممنوع) + fts.ts خودترمیم (ensure شمارشی هر جستجو → rebuild) + fts-sql.ts مشترک سرور/seed (کلاینت ساختاری FtsDbClient — نه server-only که در bun خام throw می‌کند) + قلاب upsert در createLetter + rebuild در seed/seed-big؛ نرمال‌سازی دوسویه normalizeFaText (ك/ي عربی، ارقام فارسی/عربی/لاتین، ZWNJ→فاصله)؛ توکن ≥۲ نویسه، حرف پیشوند*، رقم دقیق؛ شماره نمایشی «سال/شماره» هم قابل جستجو؛ عقب‌گرد contains در نبود توکن/خطا — جستجو هرگز نمی‌شکند؛ CSV هم همان FTS (آینه where) با hydration قطعه‌ای ۵۰۰تایی بدون orderBy (درس ۲۳)؛ هایلایت `HighlightFa` regex یونیکدی واریانت‌آگاه (ك↔ک، ي↔ی، ارقام) در ستون موضوع/طرف؛ کارایی: «مهر» ۸۶ms و «استعلام» ۵۳ms روی ۱۰٬۲۴۸ نامه (هدف <۲۰۰ms)؛ ۵۱ سنجه API/DB + ۱۴ E2E سبز |

## E-P2.2 — شماره‌گذاری و چاپ

| ID | وظیفه | معیار پذیرش | وابستگی | اندازه | وضعیت |
|---|---|---|---|---|---|
| P2-T6 | نمایش شماره با سال: «۱۴۰۵/۴۲» در همه نماها (قالب واحد از DocCounter) | همه ارجاع‌های نمایشی قالب واحد | G0 | S | **انجام** (هسته faDocNumber(num, date?) در jalali.ts — سال جلالی از تاریخ ثبت سند؛ قرارداد تضمینی: سال DocCounter سالانه است پس سال سند = سال جلالیِ createdAt، تست تطبیق جلالی/شمارنده سبز؛ اعمال در هر ۴ ارجاع نمایشی: نشان رکورد + توست ثبت + عنوان تب جامه‌ویژه + hint پالت فرمان؛ E2E: توست «شماره نامه: ۱۴۰۵/N» + نشان رکورد سبز |
| P2-T7 | چاپ نامه صادره با سربرگ شرکت (پیش‌نمایش + چاپ A4 با لوگو/شماره/تاریخ جلالی) | خروجی print از مرورگر؛ سربرگ per-company قابل تنظیم متن | P2-T6 | M | **انجام** (۱۴۰۵/۰۶/۱۱ در P2.5-U7 — پورتال LetterPrintDialog + کاغذ A4 + سربرگ/پاورقی CompanySetting + سجل PRINT؛ ۵۱/۵۱ سنجه) |
| P2-T8 | شماره‌گذاری پیکربندی‌پذیر per-type (پیشوند/پسوند/سری جدا وارده-صادره) | تنظیم در settings ادمین؛ پیش‌فرض = رفتار فعلی | P2-T6 | M |
| P2-T9 | عطف/ارتباط نامه‌ها (relationLetterId + نمایش زنجیره در جزئیات) | عطف دوسویه؛ حلقه خودارجاع ممنوع | G0 | M |

## E-P2.3 — مهلت و پیگیری

| ID | وظیفه | معیار پذیرش | وابستگی | اندازه | وضعیت |
|---|---|---|---|---|---|
| P2-T10 | فیلد مهلت روی هر ارجاع (ارجاع با مهلت اختصاصی) + نمایش در timeline | مهلت گام در timeline؛ رنگ قرمز/کهربایی | P2-T4 | S | **انجام** (۱۴۰۵/۰۶/۱۲ — deadlineAt روی LetterReferral در actOnLetter فقط REFER (آینه الگوی answerText؛ اعتبارسنجی جلالی سروری + خطای فارسی) + ارجاع اولیه مهلت نامه را به ارث می‌برد + دیت‌پیکر مهلت در پنل ارجاع با هشدار گذشته + چیپ «مهلت گام» در تب گردش با لحن قرمز (گذشته)/کهربایی (≤۳ روز) روی گام زنده + سطر «مهلت گام جاری» در شناسنامه + stepDeadlineAt در فهرست (پاکت استاندارد) — مبنای کارتابل/پیش‌نمایش «مهلت مؤثر» = گام جاری ?? نامه) |
| P2-T11 | یادآور خودکار مهلت (پردازشگر دوره‌ای: ۳ روز قبل و روز موعد → notify دارنده) | رویداد تسته با تاریخ ساختگی؛ عدم اسپم (یک‌بار به‌ازای نامه) | P0-T19 | M | **انجام** (۱۴۰۵/۰۶/۱۲ — runner ماژولی `deadline-reminder` ساعتی (رجیستری globalThis + ثبت استاتی + runJobOnce برای اجرای دستی) + ردیف ScheduledJob idempotent + دو نقطه یادآور: T3 (۱..۳ روز مانده) و DUE (روز موعد) بر مهلت مؤثر (گام ?? نامه) + **عدم اسپم ساختاری**: Notification.dedupKey یکتا (`deadline-reminder:<نامه>:<ref|letter>:<نقطه>`) با رد P2002 در notify() — حتی با اجرای هم‌زمان؛ اجرای دستی ادمین از حاکمیت (POST /api/platform/jobs/run + دکمه + سجل JOB_RUN)؛ ۴۰ سنجه API + تست E2E مرورگری) |
| P2-T12 | نمای «نامه‌های معطل» برای مدیر (باز + بدون اقدام > X روز، X قابل تنظیم) | فیلتر سرصفحه؛ سنجه در داشبورد گیت | P2-T11 | M |
| P2-T13 | گزارش هفتگی کارتابل (خلاصه تعداد ورود/اقدام/معطل به‌ازای کاربر — فقط ادمین) | خروجی MD/چاپ ساده؛ داده از audit | P1-T15 | S | **انجام** (۱۴۰۵/۰۶/۱۲ — buildCartableWeeklyReport + GET /api/letters/weekly-report: preset این‌هفته/هفته‌گذشته + staleDays + ?format=md؛ منبع داده LetterReferral سجل گردش است (چه/کی/کی هر گام) — AuditLog پیوند گیرنده REFER تاریخی/ناقص دارد؛ گارد requireSettingsAdmin؛ کارت گزارش در تنظیمات←یکپارچه‌سازی با جدول + رونوشت MD + دانلود .md + پورتال چاپ (الگوی U7)؛ ۴۸ سنجه API + ۳۰ E2E) |

## E-P2.4 — دستیار هوشمند نسل ۲

| ID | وظیفه | معیار پذیرش | وابستگی | اندازه |
|---|---|---|---|---|
| P2-T14 | ویرایش پیشنهاد AI قبل از اعمال (طبقه‌بندی/خلاصه قابل ویرایش در کارت) | اعمال مقادیر ویرایش‌شده؛ audit با مقادیر نهایی | G0 | S | **انجام** (۱۴۰۵/۰۶/۱۲ — کارت پیشنهاد ویرایش‌پذیر: Select طبقه (۷ گزینه مجاز از ai-categories.ts مشترک سرور/کلاینت) + Textarea خلاصه (سقف ۶۰۰ + CharCount) + دکمه بازگردانی + هشدار کهربایی «نسخه ویرایش‌شده اعمال می‌شود»؛ سرویس: گارد فهرست مجاز + خلاصه الزامی/سقف + سجل AI_APPLY با مقادیر نهایی (طبقه+خلاصه)؛ ۴۰۳ سیاست SECRET طبق SC-006 رفع شد) |
| P2-T15 | تاریخچه پیشنهادها (جدول AiSuggestion + نمایش نسخه‌ها) | هر درخواست یک رکورد؛ مقایسه نسخه‌ها | P2-T14 | M |
| P2-T16 | OCR نامه اسکن‌شده: پیوست تصویر → استخراج متن → پیش‌پرکردم فرم ثبت (HITL) | دقت قابل قبول روی ۵ نمونه فارسی؛ پیش‌نویس قابل ویرایش | P2-T3 | L | **انجام** (۱۴۰۵/۰۶/۱۳ — دو مرحله: tesseract محلی `-l fas --psm 6` از ocr.ts بی‌حالت (بسته زبان در git `.tessdata/`) + ساختاردهی LLM از runAiJson (task=letter.ocr-structure) با قاعده قطعی طبقه‌بندی وارده/صادره/داخلی + حذف مکانیکی سربرگ از body؛ HITL: متن‌ها فقط فیلد خالی را پر می‌کنند (merge نه replace) + هشدار بازبینی + هرگز ثبت خودکار؛ سیاست «سری»: توکن در متن خام = عدم ارسال به LLM؛ خاموشی LLM = متن خام + دکمه درج دستی؛ اسکن به صف پیوست اضافه و همراه نامه ثبت می‌شود؛ فلگ‌های letters.ocr/ai.letter-ocr + سجل OCR + تلمتری؛ ~۵۰ سنجه API (دقت ۵ نمونه = ۱۰۰٪ توکن‌های کلیدی) + ۲۲ E2E با LLM زنده) |
| P2-T17 | پیشنهاد طبقه‌بندی خودکار هنگام ثبت (پس از تایپ موضوع — بدون ذخیره) | پیشنهاد غیرقابل ذخیره در فرم؛ بدون ارسال بدنه SECRET | P2-T14 | M |

## E-P2.5 — کارتابل پیشرفته

| ID | وظیفه | معیار پذیرش | وابستگی | اندازه |
|---|---|---|---|---|
| P2-T18 | اقدام‌های گروهی (بایگانی/ارجاع چند نامه انتخاب‌شده) | انتخاب چندگانه + تأیید AlertDialog؛ audit گروهی | P1-T1 | M |
| P2-T19 | ستون‌های DataGrid نامه‌ها (شماره/نوع/وضعیت/مهلت/دارنده/شرکت) + فیلتر ترکیبی | مرتب‌سازی همه ستون‌ها؛ فیلتر ذخیره‌شده per-کاربر | P1-T1 | M |
| P2-T20 | خروجی Excel فهرست نامه‌ها (اکسل فارسی RTL با فیلترهای فعال) | CSV/XLSX با UTF-8 BOM؛ ستون‌های نمای فعال | P1-T12 | S | **انجام** (۱۴۰۵/۰۶/۱۱ در P2.5-U7 — exportLettersCsv با آینه دقیق where فهرست + box/type/status/urgency/q فعال) |
| P2-T21 | بایگانی موضوعی (طبقه درخ bایگانی + برچسب‌ها) | درخت طبقات ادمین؛ برچسب چندگانه روی نامه | P2-T18 | M |

## E-P2.6 — دبیرخانه‌ی دیجیتال

| ID | وظیفه | معیار پذیرش | وابستگی | اندازه |
|---|---|---|---|---|
| P2-T22 | الگوی نامه (template) با متغیرهای {{مشتری}}/{{تاریخ}} برای پرکاربردها | ۵ الگوی پیش‌فرض؛ درج در فرم ثبت | P2-T4 | M |
| P2-T23 | دریافت نامه از ایمیل دبیرخانه (poll IMAP دوره‌ای → پیش‌نویس نامه + HITL) | پیکربندی ادمین؛ نامه‌ها پیش‌نویس می‌شوند | P0-T19 | L |
| P2-T24 | امضای تصویری دبیرخانه روی چاپ (مکان امضا در سربرگ) | تصویر امضا per-کاربر آپلود؛ فقط در چاپ | P2-T7 | S |
| P2-T25 | قواعد محرمانگی: نامه CONFIDENTIAL/SECRET فقط برای دارنده و سازنده (نه کل شرکت) | تست متقاطع؛ SPEC security به‌روزر | P1-T31 | M |
| P2-T26 | نمای آمار دبیرخانه (روند ثبت/ارجاع/پاسخ به‌تفکیک کاربر و ماه جلالی) | نمودار در settings ادمین؛ داده از referrals | P2-T13 | M |

## نکات گسترش

- هر نوع گزارش P2-T13/T26 = زیروظیفه (بازه/گروه‌بندی/قالب).
- OCR: هر قالب نامه متفاوت (رسمی/غیررسمی) = پرامپت و اعتبارسنجی جدا.
- الگوها: هر دپارتمان کتابخانه الگوی خودش (حداقل ۱۵ الگوی پرکاربرد اداری ایرانی).
- ایمپورت تاریخی نامه‌ها از اکسل دبیرخانه (ماپینگ ستون + پیش‌نمایش + خطای سطری).

## ریسک‌ها

| ریسک | پاسخ |
|---|---|
| FTS5 روی SQLite بیلد‌شده؟ | بررسی compile flags پیش از شروع؛ جایگزین: LIKE + ایندکس + صفاهای جستجو |
| پیوست‌ها حجم DB را باد می‌کنند | سقف حجم/تعداد + لاگ پاکسازی دوره‌ای |
| OCR کیفیت فارسی | فقط پیش‌پرکردم با HITL — هرگز ثبت خودکار |
